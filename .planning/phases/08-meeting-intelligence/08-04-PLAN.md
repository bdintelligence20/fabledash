---
phase: 08-meeting-intelligence
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified: [python-backend/app/utils/briefing_generator.py, python-backend/app/api/meetings.py]
autonomous: true
---

<objective>
Build the "Fable Ops Gem" — AI-powered conversion of meeting notes into formal briefs with contextual linking.

Purpose: Convert raw meeting transcripts into polished formal briefs that include client context, action items, and next steps. The "Why behind the What" linking.
Output: Briefing generator and endpoint for generating formal briefs from meetings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-meeting-intelligence/08-02-SUMMARY.md
@.planning/phases/08-meeting-intelligence/08-03-SUMMARY.md

@python-backend/app/models/meeting.py
@python-backend/app/api/meetings.py
@python-backend/app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build briefing generator and endpoint</name>
  <files>python-backend/app/utils/briefing_generator.py, python-backend/app/api/meetings.py</files>
  <action>
    1. Create python-backend/app/utils/briefing_generator.py:
       Class BriefingGenerator:
       - __init__: loads OpenAI client

       - async generate_briefing(meeting: dict, transcript_text: str | None, format: str = "formal") -> str:
         Build a rich prompt that includes:
         - Meeting title, date, participants
         - Client context (if linked to a client, fetch client details from Firestore)
         - Transcript text or meeting notes
         - Action items (if extracted)
         - Key topics (if extracted)

         Generate a formal brief using OpenAI with structure:
         - Header: Meeting title, date, participants, client
         - Executive Summary: 2-3 sentences
         - Key Discussion Points: bullet list
         - Decisions Made: if any
         - Action Items: who, what, when
         - Next Steps
         - Context: linked tasks/projects

         Support formats: "formal" (full brief), "summary" (just executive summary + action items), "dispatch" (concise for team distribution)

       - async save_briefing(meeting_id: str, content: str, format: str, user_id: str) -> MeetingBriefing:
         Store in Firestore BRIEFING_COLLECTION
         Return MeetingBriefing model

    2. Update python-backend/app/api/meetings.py:
       - Add POST /{meeting_id}/briefing — Generate a formal briefing:
         Body: BriefingRequest (meeting_id, format, include_action_items)
         Fetches meeting + transcript from Firestore
         Calls BriefingGenerator.generate_briefing()
         Saves result
         Returns: { success, data: { briefing_id, content, format } }

       - Add GET /{meeting_id}/briefings — List briefings for a meeting:
         Returns all generated briefings for this meeting

       Place these routes appropriately (before /{meeting_id} path param routes).
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.utils.briefing_generator import BriefingGenerator; print('OK')"</verify>
  <done>BriefingGenerator creates formal briefs from meetings. Endpoints for generating and listing briefings added.</done>
</task>

</tasks>

<verification>
- [ ] BriefingGenerator imports OK
- [ ] /briefing and /briefings endpoints exist
</verification>

<success_criteria>
- AI-generated formal briefs from meeting data
- Multiple format support (formal, summary, dispatch)
- Client context injection into briefs
- Briefings stored in Firestore
</success_criteria>

<output>
After completion, create `.planning/phases/08-meeting-intelligence/08-04-SUMMARY.md`
</output>
