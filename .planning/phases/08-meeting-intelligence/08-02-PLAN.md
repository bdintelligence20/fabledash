---
phase: 08-meeting-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: [python-backend/app/utils/meeting_sync.py, python-backend/app/api/meetings.py, python-backend/app/main.py]
autonomous: true
---

<objective>
Build meeting sync service and CRUD API endpoints — sync from Read AI and Fireflies, store meetings/transcripts in Firestore.

Purpose: Pull meeting data from both services and provide API endpoints for the frontend to display and manage meetings.
Output: Meeting sync service, CRUD endpoints for meetings at /meetings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-meeting-intelligence/08-01-SUMMARY.md

@python-backend/app/models/meeting.py
@python-backend/app/utils/readai_client.py
@python-backend/app/utils/fireflies_client.py
@python-backend/app/main.py
@python-backend/app/dependencies/auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build meeting sync service and API endpoints</name>
  <files>python-backend/app/utils/meeting_sync.py, python-backend/app/api/meetings.py, python-backend/app/main.py</files>
  <action>
    1. Create python-backend/app/utils/meeting_sync.py:
       Class MeetingSyncService:
       - async sync_readai() -> dict: Fetch meetings from Read AI, map to MeetingResponse, upsert in Firestore, return { synced, errors }
       - async sync_fireflies() -> dict: Fetch transcripts from Fireflies, map to MeetingResponse, upsert, return stats
       - async sync_all() -> dict: Run both syncs, return combined results
       - async _match_client(title: str, participants: list[str]) -> str | None: Best-effort client matching by name/email

    2. Create python-backend/app/api/meetings.py:
       router = APIRouter()
       All endpoints require Depends(get_current_user)

       - GET / — List meetings: params date_from, date_to, client_id, source, limit=50. Query Firestore, sorted by date desc.
       - GET /{meeting_id} — Get meeting detail with transcript if available
       - POST / — Create manual meeting entry (MeetingCreate body)
       - PUT /{meeting_id} — Update meeting (link to client, add notes, edit)
       - DELETE /{meeting_id} — Delete meeting
       - POST /sync — Trigger sync from Read AI and Fireflies (CEO only)
       - GET /{meeting_id}/transcript — Get meeting transcript
       - GET /status — Integration status: { readai_configured, fireflies_configured, last_sync }

    3. Update python-backend/app/main.py:
       - Import and include meetings_router with prefix="/meetings", tags=["meetings"]
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.main import app; routes = [r.path for r in app.routes]; meeting_routes = [r for r in routes if 'meeting' in r]; print('Meeting routes:', meeting_routes)"</verify>
  <done>MeetingSyncService syncs from Read AI and Fireflies. Meeting CRUD endpoints at /meetings. Router registered.</done>
</task>

</tasks>

<verification>
- [ ] Meeting routes registered in app
- [ ] Sync service imports OK
- [ ] CRUD endpoints exist (list, get, create, update, delete, sync, transcript, status)
</verification>

<success_criteria>
- Meeting sync from both Read AI and Fireflies
- Full CRUD for meetings
- Manual meeting creation
- Client matching for auto-linking
- Integration status endpoint
</success_criteria>

<output>
After completion, create `.planning/phases/08-meeting-intelligence/08-02-SUMMARY.md`
</output>
