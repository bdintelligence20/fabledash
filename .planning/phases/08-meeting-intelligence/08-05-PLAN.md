---
phase: 08-meeting-intelligence
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified: [src/pages/MeetingsPage.tsx, src/pages/MeetingDetailPage.tsx, src/router.tsx, src/layouts/Sidebar.tsx]
autonomous: true
---

<objective>
Build meeting intelligence frontend — meeting list page, meeting detail with transcript and briefings, integration status.

Purpose: Give the CEO a place to view meetings, read transcripts, generate briefs, and manage meeting integrations.
Output: MeetingsPage and MeetingDetailPage with full meeting intelligence UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-meeting-intelligence/08-02-SUMMARY.md
@.planning/phases/08-meeting-intelligence/08-03-SUMMARY.md

@python-backend/app/api/meetings.py
@python-backend/app/models/meeting.py
@src/router.tsx
@src/layouts/Sidebar.tsx
@src/components/ui/index.ts
@src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build meetings list page</name>
  <files>src/pages/MeetingsPage.tsx, src/router.tsx</files>
  <action>
    Create src/pages/MeetingsPage.tsx:

    **Header:**
    - Title: "Meeting Intelligence"
    - "Sync Meetings" button (POST /meetings/sync) — with loading state
    - "Add Meeting" button to manually create a meeting entry

    **Integration Status Bar:**
    - Fetch GET /meetings/status
    - Show status badges: Read AI (green if configured, gray if not), Fireflies (same)
    - Last sync date if available

    **Filter Bar:**
    - Date range selector
    - Source filter: All, Read AI, Fireflies, Manual (select/dropdown)
    - Client filter (select from clients list)
    - Search by meeting title

    **Meetings Table:**
    - Columns: Date, Title, Source (Badge), Client (if linked), Duration, Participants count, Has Transcript (check icon), Actions
    - Row click navigates to /meetings/{id}
    - Source badges: read_ai→primary, fireflies→success, manual→default
    - Sorted by date descending

    **Create Meeting Modal:**
    - Form: title, date, participants (comma-separated), client (select), notes (textarea)
    - POST /meetings on submit
    - Close and refresh list

    **Route:** /meetings (replace existing AgentsPage stub at this URL — wait, meetings should be at /meetings)
    - Add /meetings route to router.tsx

    Relative imports. UI components from ../components/ui.
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit</verify>
  <done>MeetingsPage with sync button, integration status, filter bar, meetings table, create modal. Route at /meetings.</done>
</task>

<task type="auto">
  <name>Task 2: Build meeting detail page and update sidebar</name>
  <files>src/pages/MeetingDetailPage.tsx, src/router.tsx, src/layouts/Sidebar.tsx</files>
  <action>
    1. Create src/pages/MeetingDetailPage.tsx:

    **Header:**
    - Meeting title (h1), date, duration
    - Source badge, client link (if linked)
    - Edit button (modal to update meeting details)
    - "Generate Briefing" button (POST /meetings/{id}/briefing)

    **Meeting Info Card:**
    - Participants list
    - Linked client (with link to /clients/{id})
    - Linked tasks (with links to /tasks/{id})
    - Key topics (badges)
    - Notes section

    **Tabs:**

    Tab: "Summary"
    - AI-generated summary (if available)
    - Action items list (checkboxes, non-functional for now)
    - If no summary: "Process this meeting to generate a summary" button (POST /meetings/{id}/process)

    Tab: "Transcript"
    - Fetch GET /meetings/{id}/transcript
    - Display transcript segments: speaker name (bold), text
    - Alternating background for different speakers
    - If no transcript: "No transcript available for this meeting"

    Tab: "Briefings"
    - List of generated briefings (GET /meetings/{id}/briefings)
    - Each briefing: format badge, generated date, content (rendered as formatted text)
    - "Generate New Briefing" button with format selector (formal/summary/dispatch)
    - If no briefings: "Generate a briefing from this meeting"

    2. Add /meetings/:meetingId route to router.tsx

    3. Update src/layouts/Sidebar.tsx:
    - Replace the existing "AI Agents" nav link (or add alongside it) with "Meetings" linking to /meetings
    - Or add "Meetings" to the Intelligence nav group
    - Keep existing nav structure, just add the meetings link

    Relative imports. UI components.
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit && npx vite build</verify>
  <done>MeetingDetailPage with info card, summary/transcript/briefings tabs. Route wired. Sidebar updated. Build passes.</done>
</task>

</tasks>

<verification>
- [ ] npx tsc --noEmit passes
- [ ] npx vite build succeeds
- [ ] /meetings and /meetings/:meetingId routes exist
- [ ] Sidebar includes meetings link
</verification>

<success_criteria>
- Meeting list with filtering and sync
- Meeting detail with transcript display
- Briefing generation and display
- Integration status visibility
- Manual meeting creation
- Sidebar navigation updated
</success_criteria>

<output>
After completion, create `.planning/phases/08-meeting-intelligence/08-05-SUMMARY.md`
</output>
