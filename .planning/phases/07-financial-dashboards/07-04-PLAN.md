---
phase: 07-financial-dashboards
plan: 04
type: execute
wave: 2
depends_on: ["07-02"]
files_modified: [python-backend/app/api/financial_data.py, src/pages/VolumeRatePage.tsx, src/router.tsx]
autonomous: true
---

<objective>
Build volume vs rate analysis — evaluate high-volume clients for per-hour efficiency, identify rate compression.

Purpose: Reveals whether high-volume clients are actually profitable or if volume is masking poor per-hour rates (rate compression).
Output: Backend endpoint and VolumeRatePage at /finances/volume-rate.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-financial-dashboards/07-02-SUMMARY.md

@python-backend/app/api/financial_data.py
@src/router.tsx
@src/components/ui/index.ts
@src/lib/api.ts
@src/styles/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add volume-rate analysis endpoint</name>
  <files>python-backend/app/api/financial_data.py</files>
  <action>
    Add to the existing financial_data router:

    GET /volume-rate — Volume vs rate analysis per client:
    - Query params: date_from (optional), date_to (optional)
    - Logic:
      1. Use same data as cost-benefit (clients, invoices, time logs)
      2. For each client calculate: total_hours (volume), zar_per_hour (rate), total_revenue
      3. Classify each client:
         - "high_volume_high_rate": hours > median AND zar/hr > median (stars)
         - "high_volume_low_rate": hours > median AND zar/hr <= median (rate compression risk)
         - "low_volume_high_rate": hours <= median AND zar/hr > median (efficient)
         - "low_volume_low_rate": hours <= median AND zar/hr <= median (review needed)
      4. Calculate medians for hours and zar/hr
    - Return:
      ```json
      {
        "success": true,
        "data": {
          "clients": [
            {
              "client_id": "c1", "client_name": "Acme",
              "total_hours": 120, "zar_per_hour": 415,
              "total_revenue": 50000, "classification": "high_volume_high_rate"
            }
          ],
          "medians": { "hours": 60, "zar_per_hour": 350 },
          "quadrant_counts": {
            "high_volume_high_rate": 3,
            "high_volume_low_rate": 2,
            "low_volume_high_rate": 4,
            "low_volume_low_rate": 1
          }
        }
      }
      ```
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.api.financial_data import router; routes = [r.path for r in router.routes]; print(routes)"</verify>
  <done>GET /volume-rate endpoint with client classification into 4 quadrants, median calculations.</done>
</task>

<task type="auto">
  <name>Task 2: Build volume vs rate analysis page</name>
  <files>src/pages/VolumeRatePage.tsx, src/router.tsx</files>
  <action>
    Create src/pages/VolumeRatePage.tsx:

    **Period selector** (same pattern as other financial pages)

    **Quadrant Summary (top):**
    - 4 Cards in 2x2 grid, one per quadrant:
      - "Stars" (high vol, high rate): green border, count + client names
      - "Rate Compression Risk" (high vol, low rate): red border, count + names
      - "Efficient" (low vol, high rate): blue border, count + names
      - "Review Needed" (low vol, low rate): gray border, count + names

    **Scatter Plot simulation (middle):**
    - CSS-based quadrant chart:
      - X-axis: Hours (volume), Y-axis: ZAR/Hr (rate)
      - Median lines dividing into 4 quadrants (dashed border lines)
      - Each client as a colored dot positioned proportionally
      - Dot size based on revenue
      - Quadrant labels in corners
    - Build with absolute positioning within a relative container
    - Each dot is a small rounded div positioned via inline left/bottom percentages

    **Client Detail Table (bottom):**
    - Table: Client, Hours, ZAR/Hr, Revenue, Classification Badge
    - Classification badge colors: stars→success, compression→danger, efficient→primary, review→warning
    - Sorted by revenue descending

    **Route:** /finances/volume-rate

    Relative imports. UI components.
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit && npx vite build</verify>
  <done>VolumeRatePage with quadrant summary, scatter plot, detail table. Route at /finances/volume-rate. Build passes.</done>
</task>

</tasks>

<verification>
- [ ] Backend endpoint exists
- [ ] npx tsc --noEmit passes
- [ ] npx vite build succeeds
- [ ] /finances/volume-rate route in router.tsx
</verification>

<success_criteria>
- Client classification into 4 quadrants
- Visual quadrant chart with client dots
- Rate compression identification
- Revenue-sorted detail table
</success_criteria>

<output>
After completion, create `.planning/phases/07-financial-dashboards/07-04-SUMMARY.md`
</output>
