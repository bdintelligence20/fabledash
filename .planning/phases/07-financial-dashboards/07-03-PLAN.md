---
phase: 07-financial-dashboards
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [src/pages/CashPositionPage.tsx, src/router.tsx]
autonomous: true
---

<objective>
Build cash position dashboard — real-time cash on hand from Sage data with trend visualization.

Purpose: Cash position is a critical metric for a small agency. The CEO needs to see current cash and how it's trending.
Output: CashPositionPage at /finances/cash with current balance, trend chart, and accounts breakdown.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-financial-dashboards/07-01-SUMMARY.md

@python-backend/app/api/financial_data.py
@python-backend/app/models/financial.py
@src/router.tsx
@src/components/ui/index.ts
@src/lib/api.ts
@src/styles/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build cash position page</name>
  <files>src/pages/CashPositionPage.tsx, src/router.tsx</files>
  <action>
    Create src/pages/CashPositionPage.tsx:

    **Current Cash section (top):**
    - Large display: current cash_on_hand from latest financial snapshot (ZAR format, big text-3xl or text-4xl)
    - Color: green if positive, red if negative
    - Subtitle: "As of [snapshot date]"
    - If no data: "No cash position data available. Connect Sage or upload financial reports."

    **Accounts Overview (middle):**
    - 3 StatCards in a row:
      - Cash on Hand (from snapshot)
      - Accounts Receivable (money owed TO you)
      - Accounts Payable (money YOU owe)
    - Net position: Cash + AR - AP displayed below

    **Cash Trend (bottom):**
    - Fetch GET /financial-data/revenue-trend?months=6 (snapshots have cash data)
    - Pure CSS bar chart showing cash_on_hand per month (from snapshots)
    - Color: green bars for positive months, red for negative
    - Month labels on x-axis, ZAR amounts on y-axis
    - If only one data point, show just the current value with a note

    **Outstanding Invoices Alert:**
    - If accounts_receivable > 0: show amber alert Card with "R{amount} in outstanding invoices"
    - Quick stat: overdue invoice count from /financial-data/summary

    **Route:**
    - Path: /finances/cash
    - Add to router.tsx after other /finances/* routes

    Data comes from /financial-data/summary (for current snapshot) and /financial-data/revenue-trend (for historical).
    Relative imports. UI components from ../components/ui.
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit && npx vite build</verify>
  <done>CashPositionPage with current cash display, accounts breakdown, cash trend chart, outstanding invoice alert. Route at /finances/cash. Build passes.</done>
</task>

</tasks>

<verification>
- [ ] npx tsc --noEmit passes
- [ ] npx vite build succeeds
- [ ] /finances/cash route exists
</verification>

<success_criteria>
- Current cash position in large ZAR display
- Accounts receivable/payable breakdown
- Historical cash trend chart
- Outstanding invoices alert
</success_criteria>

<output>
After completion, create `.planning/phases/07-financial-dashboards/07-03-SUMMARY.md`
</output>
