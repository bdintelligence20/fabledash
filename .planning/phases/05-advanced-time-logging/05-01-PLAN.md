---
phase: 05-advanced-time-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [python-backend/app/models/time_log.py, python-backend/app/api/time_logs.py, src/pages/TimePage.tsx]
autonomous: true
---

<objective>
Build the time entry creation form with client/task selection, start/end time pickers, auto-calculated duration display, is_billable toggle, and description field — plus add the is_billable field to the backend model and API.

Purpose: The core data entry point for all time logging in FableDash. Every downstream feature (activity log, allocation dashboard, utilization rates) depends on time entries being captured with full metadata including billable status.
Output: A fully functional time entry form on the /time page that creates time logs via the existing /time-logs API, with is_billable support added to backend model and API.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior summaries needed — time log API structure and UI component patterns:
@.planning/phases/02-core-data-layer/02-04-SUMMARY.md
@.planning/phases/03-frontend-architecture/03-02-SUMMARY.md

# Source files:
@python-backend/app/models/time_log.py
@python-backend/app/api/time_logs.py
@src/pages/TimePage.tsx
@src/components/ui/index.ts
@src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add is_billable field to time log model and API</name>
  <files>python-backend/app/models/time_log.py, python-backend/app/api/time_logs.py</files>
  <action>
    1. In `python-backend/app/models/time_log.py`:
       - Add `is_billable: bool = True` to `TimeLogBase` (defaults to billable since most agency work is billable)
       - Add `is_billable: bool | None = None` to `TimeLogUpdate` for optional partial updates
       - `TimeLogResponse` inherits from `TimeLogBase` so it gets is_billable automatically

    2. In `python-backend/app/api/time_logs.py`:
       - In `create_time_log`: add `"is_billable": body.is_billable` to the `doc_dict`
       - In `list_time_logs`: add optional `is_billable: bool | None = Query(None)` filter parameter, apply `.where("is_billable", "==", is_billable)` if provided
       - No changes needed for update/delete — Pydantic handles is_billable via model_dump(exclude_none=True)

    Keep the existing auto-duration calculation pattern unchanged. Do NOT modify the calculate_duration_minutes function.
  </action>
  <verify>cd python-backend && python -c "from app.models.time_log import TimeLogCreate, TimeLogResponse; t = TimeLogCreate(date='2026-01-01', client_id='c1', description='test', start_time='09:00', end_time='10:00'); assert t.is_billable == True; print('Model OK')"</verify>
  <done>is_billable field exists on TimeLogBase (default True), TimeLogUpdate (optional), and TimeLogResponse; list endpoint accepts is_billable filter param</done>
</task>

<task type="auto">
  <name>Task 2: Build time entry form on TimePage</name>
  <files>src/pages/TimePage.tsx</files>
  <action>
    Replace the TimePage stub with a full time entry form. Use UI components from `src/components/ui/index.ts` (Button, Card, Input, Select, Modal) and `apiClient` from `src/lib/api.ts`.

    Form fields (inside a Card component):
    - **Date**: Input type="date", defaults to today
    - **Client**: Select dropdown — fetch clients from GET /clients on mount, display as options with client name, value is client ID
    - **Task**: Select dropdown — fetch tasks from GET /tasks (optionally filtered by selected client_id via query param) on client selection, display task name, value is task ID. Optional field.
    - **Description**: Input type="text", required, placeholder "What did you work on?"
    - **Start Time**: Input type="time", required
    - **End Time**: Input type="time", required
    - **Duration**: Read-only display that auto-calculates from start/end times client-side (difference in hours and minutes, e.g. "1h 30m"). Show "—" if start/end not both set or end <= start.
    - **Billable**: Checkbox/toggle, default checked. Label "Billable"
    - **Submit button**: "Log Time" — POST to /time-logs with all fields, show loading state via Button's loading prop

    State management:
    - Use React useState for all form fields
    - On successful submit: clear form fields (except date and client for quick re-entry), show success feedback (brief green text or reset)
    - On error: display error message from ApiError

    Layout: Page title "Time Tracking" at top, form in a Card below. Keep it simple — no tabs or complex layout yet (the list view is in 05-03).

    Import pattern: `import { Button, Card, Input, Select } from '../components/ui'` and `import apiClient from '../lib/api'` (relative imports, NOT @/ aliases — project has no path aliases configured per 03-04 decision).
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit</verify>
  <done>TimePage renders a time entry form with all fields; form submits to POST /time-logs; duration auto-calculates client-side; client/task dropdowns fetch from API; is_billable toggle present</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd python-backend && python -c "from app.models.time_log import TimeLogCreate; print('import OK')"` passes
- [ ] `cd /Users/nic/fable && npx tsc --noEmit` passes
- [ ] `cd /Users/nic/fable && npx vite build` succeeds
- [ ] TimePage.tsx imports only from ../components/ui and ../lib/api (relative paths)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- is_billable field added to backend model with default True
- Time entry form renders with all fields (date, client, task, description, start/end time, duration display, billable toggle)
- Form submits to POST /time-logs and handles success/error states
- Duration auto-calculates client-side from start/end time inputs
</success_criteria>

<output>
After completion, create `.planning/phases/05-advanced-time-logging/05-01-SUMMARY.md`
</output>
