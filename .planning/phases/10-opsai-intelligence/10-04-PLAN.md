---
phase: 10-opsai-intelligence
plan: 04
type: execute
wave: 2
depends_on: ["10-02"]
files_modified: [src/components/dashboard/AlertsPanel.tsx, src/pages/DashboardPage.tsx]
autonomous: true
---

<objective>
Wire proactive alerts into the dashboard — replace mock alerts with real data from OpsAI proactive engine.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Wire real alerts into dashboard</name>
  <files>src/components/dashboard/AlertsPanel.tsx, src/pages/DashboardPage.tsx</files>
  <action>
    1. Update src/components/dashboard/AlertsPanel.tsx:
       - Remove hardcoded mock alerts
       - Accept props: alerts (array of { type, severity, message, entity_id })
       - Display real alerts with severity-based styling:
         - high/critical: danger/red left border, danger Badge
         - medium: warning/amber left border, warning Badge
         - low: primary/blue left border, primary Badge
       - Each alert: severity badge, type label, message text
       - "View Details" link for alerts with entity_id (link to relevant page)
       - If no alerts: "All clear — no alerts at this time" with green checkmark
       - Show alert count in header

    2. Update src/pages/DashboardPage.tsx:
       - Replace mock data with real API calls:
         - Fetch GET /opsai/alerts for alerts panel
         - Fetch GET /financial-data/summary for financial StatCards
         - Fetch GET /time-logs/utilization for utilization metric
       - Update MetricRow to use real data (or show "—" if no data)
       - Update RecentActivity to fetch real recent time logs (GET /time-logs?limit=5)
       - Keep QuickActions as-is (they're navigation links)
       - Handle loading states with Spinner

    Relative imports. UI components.
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit && npx vite build</verify>
  <done>Dashboard wired to real APIs. AlertsPanel shows proactive intelligence. MetricRow shows live data.</done>
</task>
</tasks>

<output>Create .planning/phases/10-opsai-intelligence/10-04-SUMMARY.md</output>
