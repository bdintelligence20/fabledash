---
phase: 10-opsai-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [python-backend/app/utils/opsai_engine.py, python-backend/app/api/opsai.py, python-backend/app/main.py]
autonomous: true
---

<objective>
Build the OpsAI data aggregation layer and conversational API — unified queries across time logs, financials, clients, transcripts, agents.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build OpsAI engine and conversational API</name>
  <files>python-backend/app/utils/opsai_engine.py, python-backend/app/api/opsai.py, python-backend/app/main.py</files>
  <action>
    1. Create python-backend/app/utils/opsai_engine.py:
       Class OpsAIEngine:
       - __init__: OpenAI client, Firestore client
       - async query_data(question: str) -> dict:
         Parse the question to determine which data sources to query. Use OpenAI function-calling to select tools:
         - get_utilization(date_from, date_to): query time logs
         - get_revenue(period): query financial snapshots
         - get_client_info(client_name): query clients
         - get_recent_meetings(days): query meetings
         - get_overdue_tasks(): query tasks
         - get_cash_position(): query latest snapshot
         - get_top_clients(metric, limit): cost-benefit data

         Define these as OpenAI function tools. Execute the selected functions against Firestore. Return results.

       - async generate_answer(question: str, data: dict) -> str:
         Use OpenAI to generate a natural language answer from the data. Include ZAR formatting hints.

       - async ask(question: str) -> dict:
         Orchestrate: query_data → generate_answer. Return { answer, data_sources, query_tools_used }

       - get_opsai_engine() singleton

    2. Create python-backend/app/api/opsai.py:
       router = APIRouter(), Depends(get_current_user)
       - POST /ask — Ask OpsAI a question. Body: { question: str }. Returns { answer, sources }
       - GET /suggested-questions — Return a list of example questions the CEO might ask
       - GET /status — OpsAI status (openai configured, data sources available)

    3. Update main.py: include opsai_router prefix="/opsai", tags=["opsai"]
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.main import app; routes = [r.path for r in app.routes]; print([r for r in routes if 'opsai' in r])"</verify>
  <done>OpsAI engine with function-calling tools for cross-source queries. Conversational API at /opsai.</done>
</task>
</tasks>

<output>Create .planning/phases/10-opsai-intelligence/10-01-SUMMARY.md</output>
