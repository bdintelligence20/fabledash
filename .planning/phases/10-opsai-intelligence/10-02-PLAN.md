---
phase: 10-opsai-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [python-backend/app/utils/proactive_engine.py, python-backend/app/api/opsai.py]
autonomous: true
---

<objective>
Build proactive intelligence engine — scheduled analysis detecting over-servicing, utilization drops, cash position alerts, scope creep.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build proactive intelligence engine and alert endpoints</name>
  <files>python-backend/app/utils/proactive_engine.py, python-backend/app/api/opsai.py</files>
  <action>
    1. Create python-backend/app/utils/proactive_engine.py:
       Class ProactiveEngine:
       - __init__: Firestore client, OpenAI client (optional)
       - async detect_over_servicing() -> list[dict]:
         Find clients where billable hours exceed revenue expectations (high hours, low ZAR/Hr)
         Return [{client_id, client_name, hours, expected_hours, severity, message}]
       - async detect_utilization_drops() -> list[dict]:
         Compare current week's utilization to 4-week average. Flag if >15% drop.
       - async detect_cash_alerts() -> list[dict]:
         Check latest snapshot: low cash warning, high AR alert, AP due soon
       - async detect_scope_creep() -> list[dict]:
         Tasks with excessive time logs relative to initial estimates or task count growing rapidly
       - async detect_deadline_risks() -> list[dict]:
         Tasks approaching deadline with low completion indicators
       - async run_all_checks() -> dict:
         Run all detection methods, combine, assign priorities.
         Return { alerts: [...], summary: str, checked_at: str }
       - async generate_insight_summary(alerts: list) -> str:
         Use OpenAI to create a human-readable summary of all alerts

    2. Add to python-backend/app/api/opsai.py:
       - GET /alerts — Run all proactive checks, return alerts
       - GET /alerts/summary — Run checks + generate AI summary
       - POST /alerts/configure — Set alert thresholds (CEO only). Body: { utilization_threshold, cash_warning_level, etc. }
         Store config in Firestore "opsai_config" document
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.utils.proactive_engine import ProactiveEngine; print('OK')"</verify>
  <done>ProactiveEngine with 5 detection methods. Alert and summary endpoints added to OpsAI API.</done>
</task>
</tasks>

<output>Create .planning/phases/10-opsai-intelligence/10-02-SUMMARY.md</output>
