---
phase: 10-opsai-intelligence
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: [src/pages/OpsAIPage.tsx, src/router.tsx]
autonomous: true
---

<objective>
Build OpsAI conversational interface frontend — chat UI where CEO asks business questions and gets data-backed answers.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build OpsAI chat page</name>
  <files>src/pages/OpsAIPage.tsx, src/router.tsx</files>
  <action>
    Create src/pages/OpsAIPage.tsx:

    **Layout:** Full-width chat interface (not sidebar chat — this is the main OpsAI page)

    **Header:**
    - "OpsAI" title with sparkle/brain icon
    - Status indicator (GET /opsai/status — green if configured)
    - "What would you like to know about your business?"

    **Suggested Questions (shown when no messages):**
    - Fetch GET /opsai/suggested-questions
    - Display as clickable cards/pills
    - Examples: "What's our utilization this month?", "Which clients are most profitable?", "Show me cash position", "Any overdue tasks?"
    - Click fills input and auto-sends

    **Message Area:**
    - Scrollable container
    - User messages: right-aligned, primary bg
    - OpsAI responses: left-aligned, white bg with subtle border
    - OpsAI responses may contain: formatted text, currency values (ZAR), lists, emphasis
    - Show data sources used (small badges below response)
    - Typing/thinking indicator while processing

    **Input Area:**
    - Textarea with "Ask OpsAI anything..." placeholder
    - Send button
    - Enter to send, Shift+Enter for newline
    - POST /opsai/ask on send
    - Maintain local message history (no persistence needed — fresh each session)

    **Route:** /opsai
    Add to router.tsx. Also update the DashboardPage "Ask OpsAI" quick action to link to /opsai.

    Relative imports. UI components from ../components/ui.
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit && npx vite build</verify>
  <done>OpsAI chat page with suggested questions, message display, input area. Route at /opsai.</done>
</task>
</tasks>

<output>Create .planning/phases/10-opsai-intelligence/10-03-SUMMARY.md</output>
