---
phase: 09-ai-agent-ecosystem
plan: 06
type: execute
wave: 3
depends_on: ["09-01", "09-03", "09-04"]
files_modified: [python-backend/app/utils/client_agent.py, python-backend/app/api/agents.py]
autonomous: true
---

<objective>
Build Tier 2 Client-Based Agents — dedicated per-client agents with client-specific document context, repeatable task execution.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build client agent with context and task execution</name>
  <files>python-backend/app/utils/client_agent.py, python-backend/app/api/agents.py</files>
  <action>
    1. Create python-backend/app/utils/client_agent.py:
       Class ClientAgent:
       - __init__(agent_config: dict): stores agent config with client_id, model, system_prompt, document_ids
       - async get_client_context() -> dict:
         Fetch from Firestore: client details, recent tasks, recent time logs, recent meetings, recent invoices
         Build rich context dict
       - async execute_task(task_description: str) -> str:
         Use RAG engine with client-scoped documents + client context
         Generate a response/deliverable for the task
       - async answer_query(query: str, conversation_history: list[dict] = []) -> dict:
         Inject client context + RAG documents into conversation
         Return { answer, sources }
       - async generate_client_report(report_type: str = "status") -> str:
         Compile client status report from all available data
         Types: "status" (current state), "financial" (revenue/hours), "activity" (recent work)

    2. Add to agents API:
       - POST /{agent_id}/execute — Execute a task with client agent. Body: { task_description }
       - POST /{agent_id}/report — Generate client report. Body: { report_type }
       - GET /{agent_id}/context — Get agent's current client context (for debugging/review)
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.utils.client_agent import ClientAgent; print('OK')"</verify>
  <done>Client agent with context loading, task execution, query answering, report generation.</done>
</task>
</tasks>

<output>Create .planning/phases/09-ai-agent-ecosystem/09-06-SUMMARY.md</output>
