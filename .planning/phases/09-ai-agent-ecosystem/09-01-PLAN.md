---
phase: 09-ai-agent-ecosystem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [python-backend/app/models/agent.py, python-backend/app/api/agents.py, python-backend/app/main.py]
autonomous: true
---

<objective>
Define agent data model and CRUD endpoints — Firestore schema for agents with parent/child hierarchy, configuration, and FastAPI endpoints.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Create agent models and CRUD endpoints</name>
  <files>python-backend/app/models/agent.py, python-backend/app/api/agents.py, python-backend/app/main.py</files>
  <action>
    1. Create python-backend/app/models/agent.py:
       - COLLECTION_NAME = "agents"
       - AgentTier enum: OPS_TRAFFIC = "ops_traffic" (Tier 1), CLIENT_BASED = "client_based" (Tier 2)
       - AgentStatus enum: ACTIVE, PAUSED, ARCHIVED
       - AgentModel enum: GPT4O = "gpt-4o", GPT4O_MINI = "gpt-4o-mini", CLAUDE_SONNET = "claude-sonnet-4-6", CLAUDE_HAIKU = "claude-haiku-4-5"
       - AgentCreate(BaseModel): name, description (str|None), tier (AgentTier), client_id (str|None — required for Tier 2), parent_agent_id (str|None), model (AgentModel = GPT4O_MINI), system_prompt (str|None), capabilities (list[str] = []), document_ids (list[str] = [])
       - AgentUpdate(BaseModel): all optional versions of above
       - AgentResponse(BaseModel): id, name, description, tier, status (AgentStatus), client_id, client_name (str|None), parent_agent_id, model, system_prompt, capabilities, document_ids, conversation_count (int = 0), created_at, updated_at, created_by

    2. Create python-backend/app/api/agents.py (REPLACE the old placeholder):
       router = APIRouter(), all Depends(get_current_user)
       - GET / — list agents, params: tier, client_id, status, limit=50
       - POST / — create agent (CEO only). Validate: Tier 2 requires client_id
       - GET /{agent_id} — get agent detail
       - PUT /{agent_id} — update agent
       - DELETE /{agent_id} — delete agent (CEO only)
       - POST /{agent_id}/activate — set status to ACTIVE
       - POST /{agent_id}/pause — set status to PAUSED

    3. Update python-backend/app/main.py:
       - Uncomment/replace the agents router include. Use prefix="/agents", tags=["agents"]
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.main import app; routes = [r.path for r in app.routes]; print([r for r in routes if 'agent' in r])"</verify>
  <done>Agent models with tier/status/model enums. Full CRUD + activate/pause endpoints.</done>
</task>
</tasks>

<output>Create .planning/phases/09-ai-agent-ecosystem/09-01-SUMMARY.md</output>
