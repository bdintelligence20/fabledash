---
phase: 09-ai-agent-ecosystem
plan: 07
type: execute
wave: 4
depends_on: ["09-05", "09-06"]
files_modified: [src/pages/AgentsPage.tsx, src/pages/AgentDetailPage.tsx, src/pages/AgentChatPage.tsx, src/router.tsx, src/layouts/Sidebar.tsx]
autonomous: true
---

<objective>
Build AI agents frontend — agent list, agent detail/config, chat interface, and update navigation.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build agents list and detail pages</name>
  <files>src/pages/AgentsPage.tsx, src/pages/AgentDetailPage.tsx, src/router.tsx</files>
  <action>
    1. Rewrite src/pages/AgentsPage.tsx from stub:
       - Header: "AI Agents", "Create Agent" button
       - Two sections: Tier 1 (Ops/Traffic) and Tier 2 (Client-Based) cards
       - Each agent card: name, description, tier Badge, status Badge (active→success, paused→warning, archived→default), model Badge, client name (Tier 2), conversation count, last activity
       - Click → /agents/{id}
       - Create Agent Modal: name, description, tier select, client select (if Tier 2), model select, system prompt textarea
       - Ops section: "Daily Summary" button (GET /agents/ops/daily-summary), "Check Alerts" button (GET /agents/ops/alerts)
       - Alert badges showing count of active alerts

    2. Create src/pages/AgentDetailPage.tsx:
       - Header: agent name, tier badge, status badge, activate/pause buttons
       - Config Card: model, system prompt, capabilities, linked documents
       - Tabs:
         - "Conversations": list of conversations (GET /chats?agent_id=X), click → /agents/{id}/chat/{convId}
         - "Documents": list of linked documents, upload new document (POST /documents with agent_id)
         - "Activity": recent executions and reports

    3. Routes: /agents (list), /agents/:agentId (detail)
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>AgentsPage with tier sections, create modal. AgentDetailPage with config, conversations, documents tabs.</done>
</task>

<task type="auto">
  <name>Task 2: Build agent chat interface and update sidebar</name>
  <files>src/pages/AgentChatPage.tsx, src/router.tsx, src/layouts/Sidebar.tsx</files>
  <action>
    1. Create src/pages/AgentChatPage.tsx:
       - Chat interface for conversing with an agent
       - Header: agent name, conversation title, back link
       - Message list: scrollable container with messages
         - User messages: right-aligned, primary bg
         - Assistant messages: left-aligned, white/surface bg
         - Each message: content, timestamp, sources (if any — collapsible)
       - Input area: textarea + send button, Enter to send (Shift+Enter for newline)
       - On send: POST /chats/{conversationId}/messages with content
       - Show typing indicator while waiting for response
       - Auto-scroll to bottom on new messages
       - "New Conversation" button that creates via POST /chats

    2. Route: /agents/:agentId/chat/:conversationId

    3. Update Sidebar: Ensure "AI Agents" link in Intelligence group goes to /agents

    Verify: npx tsc --noEmit && npx vite build
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit && npx vite build</verify>
  <done>AgentChatPage with message list, input, typing indicator. Routes wired. Sidebar updated.</done>
</task>
</tasks>

<output>Create .planning/phases/09-ai-agent-ecosystem/09-07-SUMMARY.md</output>
