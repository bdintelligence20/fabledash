---
phase: 09-ai-agent-ecosystem
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [python-backend/app/utils/document_processor.py, python-backend/app/models/document.py, python-backend/app/api/documents.py, python-backend/app/main.py]
autonomous: true
---

<objective>
Build document processing pipeline — upload, extract text from PDF/DOCX/TXT, chunk, and prepare for RAG.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Create document models, processor, and API</name>
  <files>python-backend/app/models/document.py, python-backend/app/utils/document_processor.py, python-backend/app/api/documents.py, python-backend/app/main.py</files>
  <action>
    1. Create python-backend/app/models/document.py:
       - COLLECTION_NAME = "documents", CHUNKS_COLLECTION = "document_chunks"
       - DocumentStatus enum: PROCESSING, READY, ERROR
       - DocumentResponse(BaseModel): id, filename, file_type (str), file_size (int), status (DocumentStatus), agent_id (str|None), client_id (str|None), chunk_count (int = 0), error_message (str|None), uploaded_at, uploaded_by
       - DocumentChunk(BaseModel): id, document_id, content (str), chunk_index (int), metadata (dict = {})

    2. Create python-backend/app/utils/document_processor.py:
       Class DocumentProcessor:
       - extract_text(file_content: bytes, filename: str) -> str:
         PDF: use PyPDF2 (already in requirements). DOCX: use python-docx (already in requirements). TXT: decode utf-8.
       - chunk_text(text: str, chunk_size: int = 1000, overlap: int = 200) -> list[str]:
         Split text into overlapping chunks for RAG context.
       - async process_document(doc_id: str, file_content: bytes, filename: str) -> dict:
         Extract text, chunk, store chunks in Firestore, update document status. Return { chunk_count, word_count }.

    3. Create python-backend/app/api/documents.py (REPLACE old placeholder):
       router = APIRouter(), all Depends(get_current_user)
       - POST / — upload document (UploadFile). Optional: agent_id, client_id. Process synchronously.
       - GET / — list documents, params: agent_id, client_id, status, limit=50
       - GET /{document_id} — get document detail
       - GET /{document_id}/chunks — get document chunks
       - DELETE /{document_id} — delete document + chunks (CEO only)

    4. Update python-backend/app/main.py:
       - Uncomment/replace documents router. prefix="/documents", tags=["documents"]
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.main import app; routes = [r.path for r in app.routes]; print([r for r in routes if 'document' in r])"</verify>
  <done>Document model, processor with text extraction and chunking, CRUD API.</done>
</task>
</tasks>

<output>Create .planning/phases/09-ai-agent-ecosystem/09-02-SUMMARY.md</output>
