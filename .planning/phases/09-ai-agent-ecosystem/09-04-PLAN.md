---
phase: 09-ai-agent-ecosystem
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: [python-backend/app/models/chat.py, python-backend/app/api/chats.py, python-backend/app/main.py]
autonomous: true
---

<objective>
Build agent chat system — conversation interface with message history, context injection from RAG, streaming responses.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Create chat models and conversation API</name>
  <files>python-backend/app/models/chat.py, python-backend/app/api/chats.py, python-backend/app/main.py</files>
  <action>
    1. Create python-backend/app/models/chat.py:
       - COLLECTION_NAME = "conversations", MESSAGES_COLLECTION = "messages"
       - MessageRole enum: USER = "user", ASSISTANT = "assistant", SYSTEM = "system"
       - ChatMessage(BaseModel): id, conversation_id, role (MessageRole), content (str), sources (list[dict] = []), created_at (str)
       - ConversationCreate(BaseModel): agent_id (str), title (str|None)
       - ConversationResponse(BaseModel): id, agent_id, agent_name (str|None), title (str|None), message_count (int = 0), last_message_at (str|None), created_at, created_by

    2. Create python-backend/app/api/chats.py (REPLACE old placeholder):
       router = APIRouter(), all Depends(get_current_user)

       Conversation endpoints:
       - GET / — list conversations, params: agent_id, limit=50
       - POST / — create conversation (ConversationCreate body)
       - GET /{conversation_id} — get conversation detail
       - DELETE /{conversation_id} — delete conversation + messages

       Message endpoints:
       - GET /{conversation_id}/messages — get messages for conversation, ordered by created_at
       - POST /{conversation_id}/messages — send message to agent:
         Body: { content: str }
         Logic:
         1. Save user message to Firestore
         2. Fetch agent config (system_prompt, model, document_ids)
         3. Fetch conversation history (last 20 messages for context)
         4. Use RAGEngine to query with agent's documents as context scope
         5. Save assistant response to Firestore
         6. Update conversation (message_count, last_message_at)
         7. Return { user_message, assistant_message }

       If RAG engine not available (no OpenAI key), return error message as assistant response.

    3. Update python-backend/app/main.py:
       - Uncomment/replace chats router. prefix="/chats", tags=["chats"]
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.main import app; routes = [r.path for r in app.routes]; print([r for r in routes if 'chat' in r])"</verify>
  <done>Chat models, conversation CRUD, message send with RAG context injection.</done>
</task>
</tasks>

<output>Create .planning/phases/09-ai-agent-ecosystem/09-04-SUMMARY.md</output>
