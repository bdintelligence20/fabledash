---
phase: 09-ai-agent-ecosystem
plan: 05
type: execute
wave: 3
depends_on: ["09-01", "09-03", "09-04"]
files_modified: [python-backend/app/utils/ops_agent.py, python-backend/app/api/agents.py]
autonomous: true
---

<objective>
Build Tier 1 Ops/Traffic Agent — CEO-compiled briefs, dispatch to client agents, proactive alert capabilities.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build Ops/Traffic agent with dispatch and alerts</name>
  <files>python-backend/app/utils/ops_agent.py, python-backend/app/api/agents.py</files>
  <action>
    1. Create python-backend/app/utils/ops_agent.py:
       Class OpsTrafficAgent:
       - __init__: openai client, firestore client
       - async compile_brief(topic: str, context: dict) -> str:
         Use OpenAI to compile an ops brief from provided context (meetings, tasks, financials)
       - async dispatch_to_client_agent(agent_id: str, brief: str, instructions: str) -> dict:
         Create a conversation with the target client agent, send the brief as a message, return response
       - async check_alerts() -> list[dict]:
         Query across collections for potential alerts:
         - Overdue tasks (status != done, due_date < today)
         - Over-servicing (time logs vs budget thresholds)
         - Upcoming deadlines (tasks due within 3 days)
         - Unlinked meetings (meetings without client_id)
         Return list of alert dicts { type, severity, message, entity_id }
       - async daily_summary() -> str:
         Compile: today's meetings, overdue tasks, upcoming deadlines, recent time logs, alert count
         Format as a CEO morning briefing

    2. Add to agents API:
       - POST /ops/brief — Generate ops brief (CEO only). Body: { topic, context }
       - POST /ops/dispatch — Dispatch to client agent (CEO only). Body: { agent_id, brief, instructions }
       - GET /ops/alerts — Get current alerts
       - GET /ops/daily-summary — Get daily CEO summary
       Place these BEFORE /{agent_id} routes.
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.utils.ops_agent import OpsTrafficAgent; print('OK')"</verify>
  <done>Ops/Traffic Agent with brief compilation, agent dispatch, alert checking, daily summary.</done>
</task>
</tasks>

<output>Create .planning/phases/09-ai-agent-ecosystem/09-05-SUMMARY.md</output>
