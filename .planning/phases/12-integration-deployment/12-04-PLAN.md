---
phase: 12-integration-deployment
plan: 04
type: execute
wave: 2
depends_on: []
files_modified: [Dockerfile, docker-compose.yml, python-backend/Dockerfile, .github/workflows/deploy.yml]
autonomous: true
---

<objective>
GCP deployment pipeline — Docker configs, Cloud Build/Run configuration, environment management.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Create deployment configuration</name>
  <files>python-backend/Dockerfile, Dockerfile, docker-compose.yml</files>
  <action>
    1. Create python-backend/Dockerfile:
       - FROM python:3.11-slim
       - WORKDIR /app
       - COPY requirements.txt . && pip install --no-cache-dir -r requirements.txt
       - COPY . .
       - EXPOSE 8000
       - CMD ["gunicorn", "app.main:app", "-w", "4", "-k", "uvicorn.workers.UvicornWorker", "--bind", "0.0.0.0:8000"]

    2. Create Dockerfile (frontend):
       - Multi-stage: build stage with node:20-alpine, npm install, npm run build
       - Serve stage with nginx:alpine, copy dist to /usr/share/nginx/html
       - Copy nginx.conf for SPA routing (try_files $uri /index.html)

    3. Create docker-compose.yml for local development:
       services:
         backend:
           build: ./python-backend
           ports: ["8000:8000"]
           env_file: ./python-backend/.env
           volumes: ["./python-backend:/app"]
         frontend:
           build: .
           ports: ["3000:80"]
           depends_on: [backend]

    4. Create nginx.conf for frontend:
       - SPA routing: try_files $uri $uri/ /index.html
       - Proxy /api to backend:8000
  </action>
  <verify>ls -la /Users/nic/fable/python-backend/Dockerfile /Users/nic/fable/Dockerfile /Users/nic/fable/docker-compose.yml</verify>
  <done>Docker configs for backend and frontend. Docker Compose for local dev. Nginx config for SPA.</done>
</task>
</tasks>

<output>Create .planning/phases/12-integration-deployment/12-04-SUMMARY.md</output>
