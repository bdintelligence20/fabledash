---
phase: 12-integration-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [python-backend/app/utils/gdrive_client.py, python-backend/app/api/integrations.py, python-backend/app/config.py, python-backend/app/main.py]
autonomous: true
---

<objective>
Google Drive integration — API connection for client document access, file browsing, document linking.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build Google Drive client and API</name>
  <files>python-backend/app/utils/gdrive_client.py, python-backend/app/api/integrations.py, python-backend/app/config.py, python-backend/app/main.py</files>
  <action>
    1. Update config.py: GOOGLE_DRIVE_CREDENTIALS_PATH (str, default=""), GOOGLE_DRIVE_FOLDER_ID (str, default="")

    2. Create python-backend/app/utils/gdrive_client.py:
       Class GoogleDriveClient:
       - __init__: credentials from service account or ADC
       - async list_files(folder_id=None, query=None) -> list[dict]: List files/folders
       - async get_file(file_id) -> dict: Get file metadata
       - async search_files(query) -> list[dict]: Search by name
       - async get_client_folder(client_name) -> dict|None: Find folder matching client
       - is_configured() -> bool
       Use google-api-python-client (add to requirements.txt if needed).

    3. Create python-backend/app/api/integrations.py:
       router = APIRouter(), Depends(get_current_user)
       - GET /drive/status — connection status
       - GET /drive/files — list files (folder_id, query params)
       - GET /drive/files/{file_id} — file metadata
       - GET /drive/client/{client_id}/files — files in client's folder

    4. Update main.py: include integrations_router prefix="/integrations", tags=["integrations"]
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.main import app; print('OK')"</verify>
  <done>Google Drive client with file listing/search. Integration API endpoints.</done>
</task>
</tasks>

<output>Create .planning/phases/12-integration-deployment/12-01-SUMMARY.md</output>
