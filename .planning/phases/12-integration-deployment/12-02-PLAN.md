---
phase: 12-integration-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [python-backend/app/utils/gmail_client.py, python-backend/app/utils/calendar_client.py, python-backend/app/api/integrations.py]
autonomous: true
---

<objective>
Gmail and Calendar integration — communication pattern analysis, meeting density metrics.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build Gmail and Calendar clients</name>
  <files>python-backend/app/utils/gmail_client.py, python-backend/app/utils/calendar_client.py, python-backend/app/api/integrations.py</files>
  <action>
    1. Create python-backend/app/utils/gmail_client.py:
       Class GmailClient:
       - __init__: credentials
       - async get_email_stats(days=30) -> dict: Count emails sent/received per day, top correspondents
       - async get_client_emails(client_email, days=30) -> list[dict]: Emails with a specific client
       - async get_volume_trend(days=30) -> list[dict]: Daily email count trend
       - is_configured() -> bool

    2. Create python-backend/app/utils/calendar_client.py:
       Class CalendarClient:
       - __init__: credentials
       - async get_meetings(days_ahead=7, days_back=7) -> list[dict]: Calendar events
       - async get_meeting_density(days=30) -> dict: meetings per day average, busiest days, meeting hours
       - async get_free_slots(date) -> list[dict]: Available time slots
       - is_configured() -> bool

    3. Add to integrations API:
       - GET /gmail/status — configured check
       - GET /gmail/stats — email statistics
       - GET /gmail/volume — volume trend
       - GET /calendar/status — configured check
       - GET /calendar/meetings — upcoming/recent meetings
       - GET /calendar/density — meeting density metrics
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.utils.gmail_client import GmailClient; from app.utils.calendar_client import CalendarClient; print('OK')"</verify>
  <done>Gmail and Calendar clients. Communication stats and meeting density endpoints.</done>
</task>
</tasks>

<output>Create .planning/phases/12-integration-deployment/12-02-SUMMARY.md</output>
