---
phase: 12-integration-deployment
plan: 05
type: execute
wave: 3
depends_on: ["12-03"]
files_modified: [src/components/ui/ErrorBoundary.tsx, src/components/ui/LoadingPage.tsx, src/main.tsx]
autonomous: true
---

<objective>
Performance and error handling — error boundaries, loading states, and caching strategy.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add error boundaries and loading improvements</name>
  <files>src/components/ui/ErrorBoundary.tsx, src/components/ui/LoadingPage.tsx, src/main.tsx, src/components/ui/index.ts</files>
  <action>
    1. Create src/components/ui/ErrorBoundary.tsx:
       - React error boundary component (class component with componentDidCatch)
       - Fallback UI: Card with error message, "Reload Page" button, "Go to Dashboard" link
       - Log errors to console
       - Accept fallback prop for custom error UI

    2. Create src/components/ui/LoadingPage.tsx:
       - Full-page loading state with centered Spinner and "Loading FableDash..." text
       - Used as Suspense fallback

    3. Update src/main.tsx:
       - Wrap App with ErrorBoundary
       - Add React.Suspense with LoadingPage fallback

    4. Update src/components/ui/index.ts:
       - Export ErrorBoundary and LoadingPage
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit && npx vite build</verify>
  <done>ErrorBoundary wrapping app, LoadingPage for Suspense, exports updated.</done>
</task>
</tasks>

<output>Create .planning/phases/12-integration-deployment/12-05-SUMMARY.md</output>
