---
phase: 12-integration-deployment
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified: [src/pages/IntegrationsPage.tsx, src/router.tsx]
autonomous: true
---

<objective>
Build integrations management page — status of all external connections, communication overhead dashboard.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build integrations page</name>
  <files>src/pages/IntegrationsPage.tsx, src/router.tsx</files>
  <action>
    Create src/pages/IntegrationsPage.tsx:

    **Integration Status Cards (top):**
    - Grid of cards for each integration: Sage, Read AI, Fireflies, Google Drive, Gmail, Calendar
    - Each card: service name, icon, status Badge (Connected/Not Connected), "Configure" or "Disconnect" button
    - Fetch status from respective /status endpoints

    **Communication Overhead section (middle):**
    - Email volume: daily trend bar chart (GET /integrations/gmail/volume)
    - Meeting density: meetings per day stat, busiest day of week
    - "Email volume creep" detection: if recent weeks higher than average, show warning

    **Connected Services Detail (bottom, tabs):**
    Tab: Google Drive — file browser (list folders/files from /integrations/drive/files)
    Tab: Email Stats — top correspondents, sent/received ratio
    Tab: Calendar — upcoming meetings, free slot summary

    Route: /integrations
    Relative imports. UI components.
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit && npx vite build</verify>
  <done>IntegrationsPage with service status, communication dashboard, connected service detail.</done>
</task>
</tasks>

<output>Create .planning/phases/12-integration-deployment/12-03-SUMMARY.md</output>
