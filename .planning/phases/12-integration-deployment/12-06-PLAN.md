---
phase: 12-integration-deployment
plan: 06
type: execute
wave: 3
depends_on: ["12-03"]
files_modified: [src/layouts/Sidebar.tsx, src/pages/NotFoundPage.tsx, src/router.tsx]
autonomous: true
---

<objective>
Final polish — responsive design audit fixes, navigation completeness, 404 page improvement, production readiness.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Final polish and navigation completeness</name>
  <files>src/layouts/Sidebar.tsx, src/pages/NotFoundPage.tsx, src/router.tsx</files>
  <action>
    1. Update src/layouts/Sidebar.tsx:
       - Ensure ALL pages have navigation entries:
         Overview: Dashboard
         Manage: Clients, Tasks, Time (with sub: Log Time, Time Logs, Allocation, Utilization)
         Intelligence: OpsAI (highlighted), Finances (with sub-links), Meetings, AI Agents, Reports (with sub-links)
         System: Integrations (/integrations)
       - Add Time sub-navigation (same expandable pattern as Finances):
         - Log Time (/time)
         - Time Logs (/time/logs)
         - Allocation (/time/allocation)
         - Utilization (/time/utilization)
       - Verify responsive behavior: sidebar collapses properly on mobile

    2. Update src/pages/NotFoundPage.tsx:
       - Improve 404 page: FableDash branding, "Page Not Found" message
       - Quick links: Dashboard, Tasks, Clients, OpsAI
       - "Go Back" button using useNavigate(-1)

    3. Ensure src/router.tsx has ALL routes:
       - /integrations route for IntegrationsPage (add if missing)
       - Verify all sub-routes are present

    Relative imports. UI components.
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit && npx vite build</verify>
  <done>All navigation complete, 404 improved, responsive sidebar, all routes verified.</done>
</task>
</tasks>

<output>Create .planning/phases/12-integration-deployment/12-06-SUMMARY.md</output>
