---
phase: 11-health-vitality-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [python-backend/app/api/reports.py, python-backend/app/utils/report_engine.py, python-backend/app/main.py]
autonomous: true
---

<objective>
Build operational efficiency report backend — utilization rate, time allocation by group, saturation leaderboards, productivity score.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Create report engine and operational efficiency endpoint</name>
  <files>python-backend/app/utils/report_engine.py, python-backend/app/api/reports.py, python-backend/app/main.py</files>
  <action>
    1. Create python-backend/app/utils/report_engine.py:
       Class ReportEngine:
       - __init__: Firestore client
       - async operational_efficiency(period_start, period_end) -> dict:
         Fetch time logs, clients, tasks for period.
         Calculate: utilization_rate, time_allocation_by_group (4 partner groups), saturation_top5_clients, saturation_top5_tasks, avg_task_completion_time, productivity_score (composite metric)
       - async financial_performance(period_start, period_end) -> dict:
         Revenue growth QoQ, cost_benefit_rankings, cash_position, pass_through_analysis
       - async process_quality(period_start, period_end) -> dict:
         Task completion rates, overdue rate, meeting-to-action ratio, time_entry_consistency
       - async full_health_report(period_start, period_end) -> dict:
         Combine all three sections. Return comprehensive report data.

    2. Create python-backend/app/api/reports.py:
       router = APIRouter(), Depends(get_current_user)
       - GET /operational-efficiency — params: period_start, period_end
       - GET /financial-performance — params: period_start, period_end
       - GET /process-quality — params: period_start, period_end
       - GET /health — full health & vitality report. params: period_start, period_end

    3. Update main.py: include reports_router prefix="/reports", tags=["reports"]
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.main import app; routes = [r.path for r in app.routes]; print([r for r in routes if 'report' in r])"</verify>
  <done>Report engine with 4 report types. API endpoints for each.</done>
</task>
</tasks>

<output>Create .planning/phases/11-health-vitality-reports/11-01-SUMMARY.md</output>
