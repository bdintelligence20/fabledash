---
phase: 11-health-vitality-reports
plan: 04
type: execute
wave: 2
depends_on: ["11-02"]
files_modified: [src/pages/ComparisonReportPage.tsx, src/router.tsx]
autonomous: true
---

<objective>
Build comparison report page — side-by-side quarterly and YTD comparisons with trend visualization.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build comparison report page</name>
  <files>src/pages/ComparisonReportPage.tsx, src/router.tsx</files>
  <action>
    Create src/pages/ComparisonReportPage.tsx:

    **Period Selection:**
    - Mode toggle: "Quarter vs Quarter" or "Year to Date"
    - QvQ mode: two quarter selectors (year + quarter dropdowns)
    - YTD mode: year selector (compares current YTD vs same period last year)

    **Side-by-Side Comparison:**
    - Two-column layout: Period A (left) vs Period B (right)
    - For each metric: value in each period + delta badge (green up / red down)

    **Metrics compared (grouped):**
    Operational:
    - Utilization Rate
    - Total Hours Logged
    - Billable Hours

    Financial:
    - Total Revenue (ZAR)
    - Average ZAR/Hr
    - Net Profit (ZAR)

    Process:
    - Tasks Completed
    - Overdue Rate
    - Meeting Count

    **Trend Arrows:** Large arrow indicators between periods showing direction of change

    **Delta Summary:** Bottom section with "Improvements" (green list) and "Declines" (red list)

    Data from GET /reports/compare or /reports/quarterly or /reports/ytd

    Route: /reports/comparison

    Relative imports. UI components. ZAR formatting.
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit && npx vite build</verify>
  <done>ComparisonReportPage with side-by-side period comparison, trend arrows, delta summary.</done>
</task>
</tasks>

<output>Create .planning/phases/11-health-vitality-reports/11-04-SUMMARY.md</output>
