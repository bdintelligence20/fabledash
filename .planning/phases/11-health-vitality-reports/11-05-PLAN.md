---
phase: 11-health-vitality-reports
plan: 05
type: execute
wave: 3
depends_on: ["11-03", "11-04"]
files_modified: [python-backend/app/utils/report_export.py, python-backend/app/api/reports.py, src/pages/ReportsPage.tsx, src/layouts/Sidebar.tsx]
autonomous: true
---

<objective>
Add report export capability and update navigation for reports section.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Report export and navigation</name>
  <files>python-backend/app/utils/report_export.py, python-backend/app/api/reports.py, src/pages/ReportsPage.tsx, src/layouts/Sidebar.tsx</files>
  <action>
    1. Create python-backend/app/utils/report_export.py:
       Class ReportExporter:
       - async generate_text_report(report_data: dict) -> str:
         Format report data as structured plain text suitable for copy/paste or email
       - async generate_ai_summary(report_data: dict) -> str:
         Use OpenAI to generate an executive summary of the report (2-3 paragraphs)

    2. Add to reports API:
       - GET /export — params: period_start, period_end, format (text/summary)
         Returns { content: str, format: str }

    3. Update src/pages/ReportsPage.tsx:
       - Add "Export Report" button that calls /reports/export and displays result in a modal or copyable textarea
       - Add navigation cards for: Health Report (/reports/health), Comparison (/reports/comparison)
       - Ensure all report sub-pages are linked

    4. Update src/layouts/Sidebar.tsx:
       - Add expandable sub-links under Reports (same pattern as Finances):
         - Health & Vitality (/reports/health)
         - Period Comparison (/reports/comparison)
       - Auto-expand when on /reports/* path
  </action>
  <verify>cd /Users/nic/fable && npx tsc --noEmit && npx vite build</verify>
  <done>Report export, reports hub updated, sidebar with report sub-navigation.</done>
</task>
</tasks>

<output>Create .planning/phases/11-health-vitality-reports/11-05-SUMMARY.md</output>
