---
phase: 11-health-vitality-reports
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [python-backend/app/utils/report_comparison.py, python-backend/app/api/reports.py]
autonomous: true
---

<objective>
Build comparative framework — Q1 vs Q2 vs YTD comparison engine, period selector, trend visualization data.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build period comparison engine</name>
  <files>python-backend/app/utils/report_comparison.py, python-backend/app/api/reports.py</files>
  <action>
    1. Create python-backend/app/utils/report_comparison.py:
       Class ReportComparison:
       - __init__: report_engine (ReportEngine)
       - async compare_periods(period_a_start, period_a_end, period_b_start, period_b_end) -> dict:
         Run full_health_report for both periods.
         Calculate deltas for each metric (absolute change, percentage change).
         Flag improvements (green) and declines (red).
         Return { period_a: {...}, period_b: {...}, deltas: {...}, trends: {...} }
       - async quarterly_comparison(year: int, q1: int, q2: int) -> dict:
         Helper: calculate quarter date ranges, call compare_periods
       - async ytd_report(year: int) -> dict:
         Jan 1 to today, compared with same period last year
       - get_quarter_dates(year, quarter) -> tuple[date, date]: helper

    2. Add to reports API:
       - GET /compare — params: period_a_start, period_a_end, period_b_start, period_b_end
       - GET /quarterly — params: year, quarter_a, quarter_b
       - GET /ytd — params: year (default current year)
  </action>
  <verify>cd /Users/nic/fable/python-backend && python3 -c "from app.utils.report_comparison import ReportComparison; print('OK')"</verify>
  <done>Period comparison engine with quarterly and YTD support. Comparison endpoints.</done>
</task>
</tasks>

<output>Create .planning/phases/11-health-vitality-reports/11-02-SUMMARY.md</output>
