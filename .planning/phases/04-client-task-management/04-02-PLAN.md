---
phase: 04-client-task-management
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/pages/ClientDetailPage.tsx]
autonomous: true
---

<objective>
Build the client detail page showing client overview, associated tasks, time log summary, and financial snapshot.

Purpose: Provides a single-screen view of everything related to a client — their info, active tasks, time investment, and financial summary. This is the "client at a glance" view the CEO needs for quick context before meetings or when reviewing client health.
Output: Fully functional ClientDetailPage replacing the placeholder from 04-01.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan summary (needed — 04-01 creates the route and placeholder this plan replaces):
@.planning/phases/04-client-task-management/04-01-SUMMARY.md

# Source files:
@src/components/ui/index.ts
@src/lib/api.ts
@src/styles/tokens.ts
@python-backend/app/models/client.py
@python-backend/app/models/task.py
@python-backend/app/api/clients.py
@python-backend/app/api/tasks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build client detail page with overview and tabbed sections</name>
  <files>src/pages/ClientDetailPage.tsx</files>
  <action>
Replace the placeholder ClientDetailPage with a full client detail view:

**Data fetching:**
- Extract clientId from useParams() (react-router-dom)
- Fetch client data: apiClient.get(`/clients/${clientId}`) on mount
- Fetch client tasks: apiClient.get(`/tasks?client_id=${clientId}`) on mount
- Fetch client time logs: apiClient.get(`/time-logs?client_id=${clientId}`) on mount
- Handle loading (Spinner), error (error message with back link), and 404 (client not found) states

**Page header section:**
- Back link: "< Back to Clients" using Link to /clients
- Client name as page title (text-2xl font-bold)
- Partner group Badge next to name (same color mapping as 04-01: collab=primary, edcp=secondary, direct_clients=success, separate_businesses=warning)
- Status Badge (Active/Inactive)
- Edit button (opens inline edit modal — reuse the modal pattern from 04-01 or build a local one) and Deactivate button in header actions

**Client info Card:**
- Card component showing: contact email, contact phone, description, created date (formatted), partner group
- Two-column grid layout on desktop (md:grid-cols-2), single column mobile
- Use label/value pairs with muted labels (text-sm text-surface-500) and bold values

**Tabbed content area using Tabs component:**
- Tab 1: "Tasks" (default active) — shows tasks associated with this client
  - Mini table with columns: Title (linked to /tasks/{id} for future), Status (Badge with color: todo=secondary, in_progress=primary, in_review=warning, done=success, blocked=danger), Priority (Badge), Due Date
  - "New Task" button that links to /tasks with client pre-selected (or opens a quick-create — keep it simple, just link for now)
  - Empty state: "No tasks for this client"
  - Show task count in tab label: "Tasks (5)"

- Tab 2: "Time Logs" — shows time entries for this client
  - If time logs exist: simple Table with Date, Description, Duration columns
  - If no time logs: "No time logs recorded" placeholder message
  - Note: Time log detail/creation is Phase 5 scope. This tab is read-only display.
  - Show count in tab label: "Time Logs (3)"

- Tab 3: "Financial" — placeholder for future financial data
  - Card with message: "Financial summary will be available after Sage integration (Phase 6)"
  - Reserve layout space with min-h-[200px]

**Summary StatCards row (above tabs):**
- 3-4 StatCard components in a responsive grid (grid-cols-2 md:grid-cols-4):
  - Total Tasks: count of tasks
  - Active Tasks: count of tasks with status != "done"
  - Total Hours: sum of time log durations (duration_minutes / 60, formatted to 1 decimal)
  - Completion Rate: percentage of tasks with status "done" out of total tasks

**Important patterns:**
- Relative imports from ../components/ui and ../lib/api
- currency.format() from ../styles/tokens for any ZAR amounts (financial tab placeholder)
- Handle case where client is not found (404) — show "Client not found" with link back to /clients
- All data fetched via apiClient (auto auth token injection)
  </action>
  <verify>npx tsc --noEmit && npx vite build</verify>
  <done>ClientDetailPage shows client info, StatCards with computed metrics, tabbed Tasks/TimeLogs/Financial sections, edit modal works, back navigation to /clients list, build passes with no type errors</done>
</task>

<task type="auto">
  <name>Task 2: Add breadcrumb support for client detail route</name>
  <files>src/layouts/Breadcrumbs.tsx</files>
  <action>
Update the Breadcrumbs component to handle dynamic client detail routes:

- The current breadcrumb system uses a static route-label map. The /clients/:clientId route needs to show "Clients > {Client Name}" but the breadcrumb component doesn't have access to the client name.
- Add a pattern: for paths matching /clients/{id}, show "Clients" as a link to /clients, and the final crumb as the clientId (shortened or as-is). The client name resolution would require API calls in breadcrumbs which adds complexity — instead, use a simple approach:
  - Known routes get their label from the static map
  - Unknown segments (like a Firestore document ID) display as "Detail" or the raw segment truncated to 8 chars
  - This keeps breadcrumbs lightweight. The page title already shows the full client name.
- Ensure /clients link in breadcrumb is clickable (navigates back to list)
  </action>
  <verify>npx tsc --noEmit && npx vite build</verify>
  <done>Breadcrumbs show "Home > Clients > Detail" (or truncated ID) for /clients/:clientId routes, Clients crumb links back to /clients, no type errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx tsc --noEmit passes with zero errors
- [ ] npx vite build succeeds
- [ ] /clients/:clientId renders full detail page (not placeholder)
- [ ] Client info, tasks tab, time logs tab all render correctly
- [ ] StatCards compute metrics from fetched data
- [ ] Breadcrumbs show proper trail for client detail route
- [ ] Back link navigates to /clients
</verification>

<success_criteria>
- Client detail page loads client data, tasks, and time logs from API
- Header shows client name, partner group badge, status, edit/deactivate buttons
- Info card displays contact details in clean grid layout
- Tabs switch between Tasks, Time Logs, and Financial sections
- StatCards show Total Tasks, Active Tasks, Total Hours, Completion Rate
- Breadcrumbs work for nested /clients/:clientId route
- Loading and error states handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/04-client-task-management/04-02-SUMMARY.md`
</output>
