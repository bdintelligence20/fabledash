---
phase: 06-sage-financial-integration
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [python-backend/app/api/financial_uploads.py, python-backend/app/utils/excel_parser.py]
autonomous: true
---

<objective>
Add revenue forecast upload endpoint — parse 90-day revenue outlook documents (CSV/Excel) and store in Firestore.

Purpose: Revenue forecasts complement P&L actuals by providing forward-looking projections. The CEO uploads outlook spreadsheets that get stored and made available to financial dashboards.
Output: Revenue forecast upload endpoint with parsing and Firestore storage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sage-financial-integration/06-01-SUMMARY.md

@python-backend/app/models/financial.py
@python-backend/app/api/financial_uploads.py
@python-backend/app/utils/excel_parser.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add revenue forecast upload endpoints</name>
  <files>python-backend/app/api/financial_uploads.py, python-backend/app/utils/excel_parser.py</files>
  <action>
    1. The parse_forecast_file function should already exist in excel_parser.py from 06-03. If not, create it:
       - Read CSV or Excel file
       - Extract all rows as dicts preserving column headers
       - Expected columns: client/project name, expected revenue, expected date, confidence level, notes
       - Flexible matching like P&L parser — accept various column naming conventions
       - Return list of row dicts

    2. Add to python-backend/app/api/financial_uploads.py (same router):

       a. POST /forecast — Upload revenue forecast (CEO only):
          - Accepts multipart file upload (UploadFile)
          - Optional query param: forecast_date (str, defaults to today)
          - Parse with parse_forecast_file()
          - Create RevenueForecast document in Firestore (FORECAST_COLLECTION) with entries, filename, forecast_date
          - Return: { success: true, data: { id: str, entry_count: int, forecast_date: str } }

       b. GET /forecast — List uploaded forecasts:
          - Query params: limit (int, default 12)
          - Return summaries: { success: true, data: list of { id, filename, forecast_date, entry_count, uploaded_at } }

       c. GET /forecast/{forecast_id} — Get full forecast with entries:
          - Return: { success: true, data: RevenueForecast with all entries }

       d. DELETE /forecast/{forecast_id} — Delete forecast (CEO only):
          - Return: { success: true, message: "Forecast deleted" }
  </action>
  <verify>cd /Users/nic/fable/python-backend && python -c "from app.api.financial_uploads import router; routes = [r.path for r in router.routes]; print('Routes:', routes)"</verify>
  <done>Revenue forecast upload, list, detail, and delete endpoints. Forecast file parser handles various column formats. Stored in Firestore FORECAST_COLLECTION.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from app.api.financial_uploads import router; print('OK')"` succeeds
- [ ] Forecast endpoints (/forecast POST, GET, GET/{id}, DELETE/{id}) all exist
- [ ] parse_forecast_file function works
</verification>

<success_criteria>
- Revenue forecast file upload with flexible CSV/Excel parsing
- Forecast data stored in Firestore with entries as list of dicts
- List, detail, and delete endpoints for forecasts
- Works alongside P&L endpoints in same router
</success_criteria>

<output>
After completion, create `.planning/phases/06-sage-financial-integration/06-04-SUMMARY.md`
</output>
