---
phase: 03-frontend-architecture
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/layouts/AppLayout.tsx, src/layouts/Sidebar.tsx, src/layouts/Header.tsx, src/layouts/Breadcrumbs.tsx, src/layouts/MobileNav.tsx, src/router.tsx]
autonomous: true
---

<objective>
Redesign the app shell with a full sidebar navigation, top header bar with search and notifications, breadcrumb trail, and responsive mobile navigation — replacing the minimal placeholder AppLayout from Phase 1.

Purpose: The app shell wraps every authenticated page. It must feel like a premium operations dashboard — not a basic admin panel. The sidebar is the CEO's primary navigation surface. The header provides contextual info (breadcrumbs, search, notifications). Mobile users get a slide-out drawer instead of the sidebar. This sets the spatial foundation every feature page lives within.

Output:
- Redesigned AppLayout orchestrating Sidebar + Header + content area
- Sidebar component with branding, nav sections, collapsible state, user footer
- Header component with breadcrumbs, search input, notification bell
- Breadcrumbs component auto-derived from current route
- MobileNav component with hamburger trigger and slide-out drawer
- Updated router.tsx with route metadata for breadcrumbs
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Design tokens this plan uses for styling
@.planning/phases/03-frontend-architecture/03-01-SUMMARY.md

# Current layout and routing to be replaced
@src/layouts/AppLayout.tsx
@src/router.tsx
@src/hooks/useAuth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Sidebar, Header, Breadcrumbs, and MobileNav components</name>
  <files>src/layouts/Sidebar.tsx, src/layouts/Header.tsx, src/layouts/Breadcrumbs.tsx, src/layouts/MobileNav.tsx</files>
  <action>
**Sidebar.tsx:**
Create a full sidebar component replacing the inline sidebar in AppLayout.

Structure (top to bottom):
1. **Brand area** (top): FableDash logo/name with a tagline "Operations Intelligence" in text-xs text-surface-400. Use `text-xl font-bold` for "Fable" in primary-600 + "Dash" in surface-400 (split branding). Border-b separator.

2. **Main navigation** (scrollable middle):
   Group nav items into sections with section labels:
   - **Overview**: Dashboard (Home icon)
   - **Manage**: Clients (UserCircle), Tasks (CheckSquare), Time (Clock)
   - **Intelligence**: Finances (DollarSign), AI Agents (Bot), Reports (BarChart2)

   Each nav item: icon + label + optional badge count (e.g., notification count)
   Active state: `bg-primary-50 text-primary-600 font-semibold` with left border accent (`border-l-3 border-primary-500`)
   Inactive: `text-surface-600 hover:bg-surface-50 hover:text-surface-900`
   All items: `flex items-center gap-3 px-4 py-2.5 rounded-r-lg text-sm transition-default`

   Section labels: `text-xs font-semibold uppercase tracking-wider text-surface-400 px-4 pt-4 pb-2`

3. **Collapse toggle** (above user area): Button to collapse sidebar to icons-only mode (w-16). Store collapsed state in localStorage. When collapsed, section labels hide, nav labels hide, only icons show with tooltip on hover (use `title` attribute for now — no tooltip library).

4. **User footer** (bottom): bordered-t, user Avatar (from 03-02 if available, or inline), display name, email, sign-out button. Same pattern as current AppLayout but using the design system tokens.

Props: `collapsed` (boolean), `onToggle` (() => void), `onMobileClose` (() => void, optional)
Use icons from `lucide-react` (already installed).

**Header.tsx:**
Top bar that sits above the main content area (not the sidebar).

Structure (left to right):
1. **Mobile hamburger** (md:hidden): button that triggers MobileNav open
2. **Breadcrumbs** component (rendered inline)
3. **Spacer** (flex-1)
4. **Search input**: Compact search field (`max-w-xs`) with Search icon, placeholder "Search..." — non-functional for now (just UI). Uses design system Input styling but compact (py-1.5 px-3 text-sm rounded-lg).
5. **Notification bell**: Bell icon button with optional red dot badge for unread count — non-functional for now (placeholder). `relative` positioned, red dot is `absolute -top-1 -right-1 h-2 w-2 bg-danger-500 rounded-full`.

Props: `onMobileMenuOpen` (() => void)
Styling: `bg-white border-b border-surface-200 px-6 py-3 flex items-center gap-4`

**Breadcrumbs.tsx:**
Auto-generates breadcrumb trail from the current route.

- Use `useLocation` from react-router-dom to get current path
- Define a route-label map: `{ '/': 'Dashboard', '/clients': 'Clients', '/tasks': 'Tasks', '/finances': 'Finances', '/agents': 'AI Agents', '/time': 'Time Tracking', '/reports': 'Reports' }`
- For nested routes (future-proofing): split path and build links. E.g., `/clients/123` -> Home > Clients > [detail]
- Show "Home" as first breadcrumb (links to /), then current page name
- Separator: ChevronRight icon from lucide-react, `text-surface-300 h-4 w-4`
- Current page (last item): `text-surface-900 font-medium`, previous items: `text-surface-500 hover:text-primary-600`

**MobileNav.tsx:**
Slide-out drawer for mobile navigation.

- Props: `isOpen` (boolean), `onClose` (() => void)
- Backdrop: fixed overlay `bg-black/50 z-40`, click-to-close
- Drawer: slides in from left, `fixed inset-y-0 left-0 w-72 bg-white z-50 shadow-strong transform transition-transform` — `-translate-x-full` when closed, `translate-x-0` when open
- Renders the Sidebar content inside the drawer (import Sidebar and pass `onMobileClose={onClose}`)
- Close button (X icon) in top-right of drawer
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Sidebar (with collapsible state, nav sections, user footer), Header (with breadcrumbs, search placeholder, notification bell), Breadcrumbs (route-aware), and MobileNav (slide-out drawer) components created in src/layouts/</done>
</task>

<task type="auto">
  <name>Task 2: Rewire AppLayout to compose new shell components and update router</name>
  <files>src/layouts/AppLayout.tsx, src/router.tsx</files>
  <action>
**AppLayout.tsx — Full rewrite:**

Replace the current AppLayout with a composition of the new shell components:

```
Layout structure:
+--sidebar (fixed left, hidden on mobile)--+--main area (flex-1)---+
|  Sidebar component                       |  Header component     |
|  (w-64 when expanded, w-16 collapsed)    |  (top bar)            |
|                                          |  Content (Outlet)     |
+------------------------------------------+-----------------------+
```

Implementation:
- State: `sidebarCollapsed` (boolean, initialized from localStorage key `fabledash-sidebar-collapsed`)
- State: `mobileNavOpen` (boolean, default false)
- Persist `sidebarCollapsed` to localStorage on change
- Outer wrapper: `min-h-screen flex bg-surface-50`
- Sidebar: hidden on mobile (`hidden md:flex`), visible on desktop. Width transitions smoothly: `md:w-64` when expanded, `md:w-16` when collapsed. Use `transition-all duration-300`.
- Main area: `flex-1 flex flex-col min-w-0` (min-w-0 prevents flex child overflow)
- Header: rendered at top of main area
- Content: `flex-1 overflow-auto p-6` wrapping `<div className="max-w-7xl mx-auto w-full"><Outlet /></div>`
- MobileNav: always rendered, controlled by `mobileNavOpen` state

Remove ALL old inline sidebar code. The new AppLayout is purely a composition wrapper.

**router.tsx:**

No structural changes to routes. Keep the exact same route tree:
- /login (public)
- ProtectedRoute > AppLayout > [all 7 pages + 404]

Do NOT change route paths or page imports. The routing structure from 01-04 remains intact. Only the AppLayout rendering changes.

Verify the auth flow still works: ProtectedRoute wraps AppLayout which now uses the new shell components.
  </action>
  <verify>npm run build && npx tsc --noEmit (full build succeeds, type-check passes)</verify>
  <done>AppLayout rewritten to compose Sidebar + Header + MobileNav + Outlet. Sidebar collapse state persisted to localStorage. Mobile drawer works. Router unchanged. Auth flow intact.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `npx tsc --noEmit` passes
- [ ] AppLayout renders Sidebar on desktop, MobileNav drawer on mobile viewport
- [ ] Sidebar has 3 nav sections (Overview, Manage, Intelligence) with 7 nav items
- [ ] Sidebar collapse toggle works and persists to localStorage
- [ ] Header shows breadcrumbs, search input, notification bell
- [ ] Breadcrumbs reflect current route
- [ ] MobileNav opens/closes with backdrop
- [ ] Auth flow still works (ProtectedRoute -> AppLayout -> pages)
- [ ] All 7 existing page stubs still render in their routes
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- App shell feels premium — clean sidebar with grouped navigation, not a basic admin template
- Responsive: desktop shows sidebar, mobile shows hamburger + drawer
- Sidebar collapse is smooth and state persists across sessions
- No new dependencies added (uses lucide-react already installed)
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-architecture/03-03-SUMMARY.md`
</output>
