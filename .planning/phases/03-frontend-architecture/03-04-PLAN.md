---
phase: 03-frontend-architecture
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified: [src/pages/DashboardPage.tsx, src/components/dashboard/MetricRow.tsx, src/components/dashboard/RecentActivity.tsx, src/components/dashboard/AlertsPanel.tsx, src/components/dashboard/QuickActions.tsx]
autonomous: true
---

<objective>
Build the CEO Dashboard page skeleton with a widget grid layout containing placeholder sections for key business metrics, recent activity, alerts, and quick actions — using the UI components from 03-02 and the app shell from 03-03.

Purpose: The Dashboard is the CEO's home screen — the first thing seen after login. This skeleton establishes the spatial layout and widget structure that later phases will fill with real data (financial metrics from Phase 7, time data from Phase 5, alerts from Phase 10). The placeholders demonstrate the intended information architecture and give a tangible feel for the final product. All financial values display in ZAR with the R symbol.

Output:
- Fully laid-out DashboardPage.tsx with responsive widget grid
- MetricRow component showing 4 stat cards (revenue, utilization, active clients, cash position) with placeholder data
- RecentActivity component showing a feed of recent items
- AlertsPanel component showing operational alerts/warnings
- QuickActions component with shortcut buttons for common CEO actions
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# UI components this plan uses
@.planning/phases/03-frontend-architecture/03-02-SUMMARY.md

# App shell this page lives inside
@.planning/phases/03-frontend-architecture/03-03-SUMMARY.md

# Design tokens for styling
@.planning/phases/03-frontend-architecture/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard widget components (MetricRow, RecentActivity, AlertsPanel, QuickActions)</name>
  <files>src/components/dashboard/MetricRow.tsx, src/components/dashboard/RecentActivity.tsx, src/components/dashboard/AlertsPanel.tsx, src/components/dashboard/QuickActions.tsx</files>
  <action>
Create src/components/dashboard/ directory with the following components. All use the UI components from src/components/ui/ (import from '@/components/ui').

**MetricRow.tsx:**
A row of 4 StatCard components displaying key CEO metrics with placeholder data.

Metrics (all placeholder/mock data):
1. **Monthly Revenue**: value "R 1,247,500", change +12.5% up, icon TrendingUp (lucide), prefix "R"
2. **Utilization Rate**: value "78%", change +3.2% up, icon Activity (lucide)
3. **Active Clients**: value "24", change +2 flat (or up), icon Users (lucide)
4. **Cash Position**: value "R 271,340", change -1.8% down, icon Wallet (lucide), prefix "R"

Layout: responsive grid — `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`
Each StatCard uses the component from 03-02 with appropriate props.

Export as a single component that renders the grid of 4 cards. Use hardcoded mock data (not API calls).

**RecentActivity.tsx:**
A card showing a feed of recent business activity items.

Structure:
- Card wrapper with Card.Header "Recent Activity" + "View all" link
- Card.Body with a list of 5-6 mock activity items
- Each item: icon (colored circle), description text, timestamp (relative, e.g., "2 hours ago")

Mock items (hardcoded):
- "New client onboarded: Acme Creative" — 2h ago, success icon
- "Invoice #1247 sent to Blue Mountain Co" — 4h ago, primary icon
- "Time log submitted: 6.5h on Meridian project" — 5h ago, accent icon
- "Meeting transcript processed: Q1 Strategy Review" — Yesterday, primary icon
- "Utilization alert: Team dropped below 75%" — Yesterday, warning icon

Each item: `flex items-center gap-3 py-3 border-b border-surface-100 last:border-0`
Icon circle: `h-8 w-8 rounded-full flex items-center justify-center` with variant-appropriate bg color
Timestamp: `text-xs text-surface-400 ml-auto flex-shrink-0`

**AlertsPanel.tsx:**
A card showing operational alerts and warnings for the CEO.

Structure:
- Card with Card.Header "Alerts & Insights" + Badge showing count
- List of 3-4 mock alerts, each with severity indicator

Mock alerts:
- DANGER: "Cash position below R250K threshold" — actionable
- WARNING: "Client 'Meridian Corp' over-serviced by 23% this month"
- WARNING: "3 time logs pending review for this week"
- INFO: "Quarterly report ready for Q4 2025"

Each alert:
- Left color bar (4px wide, colored by severity: danger-500, warning-500, primary-500)
- Title (font-medium), description (text-sm text-surface-600)
- Badge component showing severity level
- Layout: `flex gap-3 p-3 rounded-lg bg-surface-50 hover:bg-surface-100 transition-default cursor-pointer`

**QuickActions.tsx:**
A card with shortcut buttons for common CEO actions.

Structure:
- Card with Card.Header "Quick Actions"
- Grid of action buttons: `grid grid-cols-2 gap-3`

Mock actions (4 buttons):
1. "Log Time" — Clock icon, secondary variant
2. "New Task" — Plus icon, secondary variant
3. "View Finances" — DollarSign icon, secondary variant
4. "Ask OpsAI" — Bot icon, primary variant (highlighted as key action)

Each action: Button component from 03-02 with icon prop, full width within grid cell. The "Ask OpsAI" button uses primary variant to stand out.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>4 dashboard widget components created: MetricRow (4 stat cards with ZAR values), RecentActivity (5-item feed), AlertsPanel (4 alerts with severity), QuickActions (4 shortcut buttons)</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite DashboardPage with widget grid layout</name>
  <files>src/pages/DashboardPage.tsx</files>
  <action>
Replace the placeholder DashboardPage with a full widget grid layout composing all dashboard components.

**Page structure:**

1. **Page header** (top):
   - Title: "Dashboard" in `text-2xl font-bold text-heading`
   - Subtitle: "Welcome back. Here's your business at a glance." in `text-body mt-1`
   - Right side: current date formatted as "Tuesday, 25 Feb 2026" using date-fns `format` function (date-fns is already in package.json)

2. **Metric row** (full width):
   - MetricRow component — `mt-6`

3. **Main content grid** (below metrics):
   - `grid grid-cols-1 lg:grid-cols-3 gap-6 mt-6`
   - Left column (lg:col-span-2): RecentActivity component (takes more space)
   - Right column (lg:col-span-1): Stack of AlertsPanel + QuickActions with `space-y-6`

4. **Bottom section** (full width, below grid):
   - `mt-6` — A placeholder Card for "Revenue Overview" chart area
   - Card.Header: "Revenue Overview" + Tabs component (from 03-02) with tabs: "Monthly", "Quarterly", "YTD"
   - Card.Body: A centered placeholder div with BarChart2 icon (large, text-surface-300) and text "Chart visualization coming in Phase 7" in text-surface-400
   - Height: `min-h-[300px]` to reserve visual space for the chart

All sections use `animate-up` for a subtle entrance animation (staggered if possible — add `style={{ animationDelay: '100ms' }}` to successive sections).

**Important:**
- Use the currency.format() helper from src/styles/tokens.ts for any ZAR display OR just use hardcoded "R 1,247,500" format since this is mock data
- Do NOT make API calls — this is a skeleton with hardcoded placeholders
- All imports from UI library via '@/components/ui'
- Dashboard components from '@/components/dashboard'
  </action>
  <verify>npm run build && npx tsc --noEmit</verify>
  <done>DashboardPage fully laid out with page header (title + date), MetricRow (4 stat cards), main grid (RecentActivity + AlertsPanel + QuickActions), and bottom chart placeholder with tab selector. All using design system components.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `npx tsc --noEmit` passes
- [ ] DashboardPage renders without errors at / route
- [ ] 4 metric StatCards display with ZAR values (R symbol) for financial metrics
- [ ] RecentActivity shows 5+ mock items with timestamps
- [ ] AlertsPanel shows alerts with color-coded severity
- [ ] QuickActions shows 4 action buttons with "Ask OpsAI" highlighted
- [ ] Revenue chart placeholder area visible with Tabs component
- [ ] Responsive: single column on mobile, multi-column grid on desktop
- [ ] Page has subtle entrance animations
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Dashboard feels like a CEO operations hub — not a generic admin dashboard
- Financial values display in ZAR with R prefix
- Layout is responsive and content doesn't overflow on any viewport
- Placeholder sections clearly indicate what real data will fill them in future phases
- No API calls — purely frontend with mock data
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-architecture/03-04-SUMMARY.md`
</output>
