---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, python-backend/requirements.txt, python-backend/app/utils/supabase_client.py, python-backend/app/main.py, python-backend/app/api/agents.py, python-backend/app/api/chats.py, python-backend/app/api/documents.py, python-backend/app/api/clients.py, python-backend/app/api/tasks.py, src/utils/api.ts]
autonomous: true
---

<objective>
Strip all Supabase dependencies from the entire codebase and install Firebase dependencies for both frontend and backend.

Purpose: Create a clean slate free of Supabase so Firebase can be set up without conflicts or dead code.
Output: Codebase with no Supabase references, Firebase packages installed, environment templates updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@package.json
@python-backend/requirements.txt
@python-backend/app/utils/supabase_client.py
@python-backend/app/main.py
@src/utils/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Supabase from backend</name>
  <files>python-backend/requirements.txt, python-backend/app/utils/supabase_client.py, python-backend/app/main.py, python-backend/app/api/agents.py, python-backend/app/api/chats.py, python-backend/app/api/documents.py, python-backend/app/api/clients.py, python-backend/app/api/tasks.py</files>
  <action>
    1. Remove `supabase` from python-backend/requirements.txt
    2. Delete python-backend/app/utils/supabase_client.py entirely
    3. In python-backend/app/main.py: remove the `from app.utils.supabase_client import get_supabase_client` import, remove the `supabase = get_supabase_client()` line, remove the `/task-statuses` endpoint that uses supabase directly. Keep the app structure, CORS, health check, and exception handler intact but comment out the router includes (they'll be rebuilt in Phase 2).
    4. In each API router file (agents.py, chats.py, documents.py, clients.py, tasks.py): gut the file contents, replacing with a placeholder FastAPI router with a single GET / endpoint returning {"status": "pending rebuild"}. Remove all supabase imports and usage. Keep the `router = APIRouter()` declaration.
    5. Add firebase-admin and google-cloud-firestore to requirements.txt
    6. Add openai to requirements.txt (keep existing)
    7. Remove the old SQL fix files from root directory (CHAT_FOREIGN_KEY_FIX.sql, COMPLETE_FABLEDASH_DATABASE_FIX.sql, CRITICAL_DOCUMENT_COLUMN_FIX.sql, DOCUMENT_UPLOAD_FIX.sql, FINAL_COMPLETE_FIX_GUIDE.md, FINAL_DOCUMENT_COLUMN_FIX.sql, FINAL_EMBEDDING_DEBUG.sql, SUPABASE_RLS_FIX.sql, VECTOR_EMBEDDING_FIX.sql) — these are Supabase artifacts no longer needed.
  </action>
  <verify>cd python-backend && python -c "from app.main import app; print('FastAPI app loads')" && cd ..</verify>
  <done>No supabase references anywhere in python-backend/. firebase-admin and google-cloud-firestore in requirements.txt. All API routers are placeholder stubs. SQL fix files removed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Supabase from frontend and install Firebase</name>
  <files>package.json, src/utils/api.ts, .env.example, python-backend/.env.example</files>
  <action>
    1. Remove `@supabase/supabase-js` from package.json dependencies
    2. Add `firebase` (^10.x) to package.json dependencies
    3. Add `react-router-dom` (^6.x) to package.json dependencies (needed for proper routing in later plans)
    4. Run npm install to update node_modules and package-lock.json
    5. In src/utils/api.ts: remove any Supabase references. Keep the fetch-based API client but update the fallback URL to http://localhost:8000. Remove the window.ENV reference (will be replaced with proper env config).
    6. Create .env.example at root with:
       VITE_API_URL=http://localhost:8000
       VITE_FIREBASE_API_KEY=
       VITE_FIREBASE_AUTH_DOMAIN=
       VITE_FIREBASE_PROJECT_ID=
       VITE_FIREBASE_STORAGE_BUCKET=
       VITE_FIREBASE_MESSAGING_SENDER_ID=
       VITE_FIREBASE_APP_ID=
    7. Create python-backend/.env.example with:
       FIREBASE_PROJECT_ID=
       FIREBASE_CREDENTIALS_PATH=./firebase-credentials.json
       OPENAI_API_KEY=
       PORT=8000
       HOST=0.0.0.0
       CORS_ORIGINS=http://localhost:5173,http://localhost:3000
       LOG_LEVEL=INFO
    8. Delete the old env-config.js from root (runtime config hack, no longer needed with Vite env vars)
  </action>
  <verify>npm install && npm run build 2>&1 | head -20</verify>
  <done>No @supabase/supabase-js in package.json. firebase and react-router-dom installed. .env.example files created for both frontend and backend. env-config.js deleted. npm install succeeds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -r "supabase" python-backend/ --include="*.py"` returns no matches (except possibly comments about migration)
- [ ] `grep -r "supabase" src/ --include="*.ts" --include="*.tsx"` returns no matches
- [ ] `grep -r "@supabase" package.json` returns no matches
- [ ] `npm install` succeeds
- [ ] `cd python-backend && python -c "from app.main import app"` succeeds
</verification>

<success_criteria>
- All Supabase dependencies and code removed from entire codebase
- Firebase packages installed (frontend: firebase, backend: firebase-admin + google-cloud-firestore)
- react-router-dom installed
- Environment templates created for both frontend and backend
- Backend still boots (FastAPI app loads with placeholder routers)
- Frontend still builds (no import errors)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md`
</output>
