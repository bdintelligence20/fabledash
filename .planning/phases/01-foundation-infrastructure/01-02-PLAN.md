---
phase: 01-foundation-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [python-backend/app/main.py, python-backend/app/utils/firebase_client.py, python-backend/app/config.py, python-backend/app/models/base.py, python-backend/server.py]
autonomous: true
---

<objective>
Restructure the FastAPI backend with Firebase Admin SDK integration, Firestore client utility, clean configuration management, and production-ready middleware.

Purpose: Establish a solid backend foundation that all future API endpoints will build on.
Output: Clean FastAPI app with Firebase/Firestore connection, config module, CORS, error handling, health checks, and logging.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md

@python-backend/app/main.py
@python-backend/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config module and Firebase client utility</name>
  <files>python-backend/app/config.py, python-backend/app/utils/firebase_client.py, python-backend/app/utils/__init__.py</files>
  <action>
    1. Create python-backend/app/config.py:
       - Use pydantic BaseSettings (from pydantic-settings, add to requirements.txt if needed) for environment variable management
       - Define Settings class with fields: FIREBASE_PROJECT_ID (str), FIREBASE_CREDENTIALS_PATH (str, default="./firebase-credentials.json"), OPENAI_API_KEY (str), PORT (int, default=8000), HOST (str, default="0.0.0.0"), CORS_ORIGINS (str, default="http://localhost:5173"), LOG_LEVEL (str, default="INFO")
       - Add a `cors_origins_list` property that splits CORS_ORIGINS by comma
       - Create a cached `get_settings()` function using lru_cache

    2. Create python-backend/app/utils/firebase_client.py:
       - Import firebase_admin, credentials, firestore
       - Create `initialize_firebase()` function that:
         a. Checks if firebase_admin is already initialized (firebase_admin._apps)
         b. If FIREBASE_CREDENTIALS_PATH exists as a file, use credentials.Certificate(path)
         c. If GOOGLE_APPLICATION_CREDENTIALS env var is set, use credentials.ApplicationDefault()
         d. Otherwise, use firebase_admin.initialize_app() with no credentials (for GCP environments where ADC works)
       - Create `get_firestore_client()` function that returns firestore.client()
       - Create a module-level `db` variable initialized lazily
       - Include proper error handling and logging

    3. Update python-backend/app/utils/__init__.py to export get_firestore_client
  </action>
  <verify>cd python-backend && python -c "from app.config import get_settings; s = get_settings(); print(f'Config loaded: port={s.PORT}')" && cd ..</verify>
  <done>Config module loads settings from env. Firebase client utility exists with initialization logic. Both importable without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure FastAPI app with clean middleware stack</name>
  <files>python-backend/app/main.py, python-backend/app/models/base.py, python-backend/server.py</files>
  <action>
    1. Rewrite python-backend/app/main.py:
       - Import config via get_settings()
       - Import initialize_firebase from utils
       - Create lifespan context manager that initializes Firebase on startup and logs shutdown
       - Create FastAPI app with title="FableDash API", description="CEO Operations Intelligence Hub", version="2.0.0", lifespan=lifespan
       - Add CORSMiddleware using settings.cors_origins_list, allow_credentials=True, allow_methods=["*"], allow_headers=["*"]
       - Add ProxyHeadersMiddleware for GCP (trusted_hosts=["*"])
       - Add request logging middleware (log method, path, status code, duration)
       - Keep placeholder router includes from 01-01 (they return "pending rebuild")
       - Root endpoint GET / returns {"name": "FableDash API", "version": "2.0.0", "status": "operational"}
       - Health check GET /health returns {"status": "healthy", "firebase": bool (check if firebase_admin._apps is not empty)}
       - Global exception handler returning structured {"success": false, "error": str, "detail": str} for all unhandled exceptions
       - DO NOT use deprecated @app.on_event decorators - use the lifespan parameter instead

    2. Update python-backend/app/models/base.py:
       - Define BaseResponse(BaseModel) with success: bool, message: str | None = None
       - Define ErrorResponse(BaseModel) with success: bool = False, error: str, detail: str | None = None
       - These will be used by all future API endpoints

    3. Update python-backend/server.py:
       - Simple entry point: import uvicorn, run app.main:app with host/port from config
       - Add reload=True for development
  </action>
  <verify>cd python-backend && python -c "from app.main import app; print(f'App: {app.title} v{app.version}')" && cd ..</verify>
  <done>FastAPI app boots cleanly with Firebase initialization in lifespan. Health check endpoint works. CORS configured from env. Base response models defined. Server entry point updated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd python-backend && python -c "from app.main import app; print('OK')"` succeeds
- [ ] `cd python-backend && python -c "from app.config import get_settings; print('OK')"` succeeds
- [ ] `cd python-backend && python -c "from app.utils.firebase_client import get_firestore_client; print('OK')"` succeeds
- [ ] `cd python-backend && python -c "from app.models.base import BaseResponse, ErrorResponse; print('OK')"` succeeds
</verification>

<success_criteria>
- FastAPI app boots with Firebase initialization
- Config loaded from environment variables via pydantic
- Firestore client utility ready for use
- CORS, error handling, health check, and logging all working
- Base response models defined for consistent API responses
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md`
</output>
