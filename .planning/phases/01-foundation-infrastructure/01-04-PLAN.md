---
phase: 01-foundation-infrastructure
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/main.tsx, src/App.tsx, src/lib/firebase.ts, src/config/env.ts, src/router.tsx, src/layouts/AppLayout.tsx, src/pages/DashboardPage.tsx, src/pages/TasksPage.tsx, src/pages/ClientsPage.tsx, src/pages/FinancesPage.tsx, src/pages/AgentsPage.tsx, src/pages/TimePage.tsx, src/pages/ReportsPage.tsx, vite.config.ts, index.html]
autonomous: true
---

<objective>
Reset the React frontend with Firebase client SDK, proper routing via react-router-dom, clean app shell layout, and page stubs for all dashboard sections.

Purpose: Establish the frontend foundation that all feature pages will build on — proper routing, Firebase connection, and app structure.
Output: Clean React app with Firebase SDK, router with all page routes, app layout shell, and placeholder pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@package.json
@src/main.tsx
@src/App.tsx
@vite.config.ts
@index.html
@tailwind.config.js
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Firebase client SDK and config modules</name>
  <files>src/lib/firebase.ts, src/config/env.ts, src/lib/api.ts</files>
  <action>
    1. Create src/config/env.ts:
       - Export a typed config object reading from import.meta.env:
         API_URL: string (VITE_API_URL, default "http://localhost:8000")
         FIREBASE_API_KEY: string (VITE_FIREBASE_API_KEY)
         FIREBASE_AUTH_DOMAIN: string (VITE_FIREBASE_AUTH_DOMAIN)
         FIREBASE_PROJECT_ID: string (VITE_FIREBASE_PROJECT_ID)
         FIREBASE_STORAGE_BUCKET: string (VITE_FIREBASE_STORAGE_BUCKET)
         FIREBASE_MESSAGING_SENDER_ID: string (VITE_FIREBASE_MESSAGING_SENDER_ID)
         FIREBASE_APP_ID: string (VITE_FIREBASE_APP_ID)

    2. Create src/lib/firebase.ts:
       - Import { initializeApp } from 'firebase/app'
       - Import { getAuth } from 'firebase/auth'
       - Import { getFirestore } from 'firebase/firestore'
       - Create firebaseConfig object from env config
       - Initialize app: const app = initializeApp(firebaseConfig)
       - Export auth = getAuth(app)
       - Export firestore = getFirestore(app)
       - Export default app

    3. Create src/lib/api.ts (replacing old src/utils/api.ts):
       - Import { auth } from './firebase'
       - Create an apiClient object with methods: get, post, put, delete
       - Each method:
         a. Gets current user's ID token via auth.currentUser?.getIdToken()
         b. Adds Authorization: Bearer {token} header
         c. Uses fetch with proper error handling
         d. Returns parsed JSON response
       - Base URL from env config
       - Support for FormData (file uploads) - skip Content-Type header when body is FormData
       - All methods throw on non-ok responses with parsed error message

    4. Delete old src/utils/api.ts and src/utils/ directory
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>Firebase SDK initialized from env config. API client with auth token injection. Old utils directory removed.</done>
</task>

<task type="auto">
  <name>Task 2: Create router, app layout, and page stubs</name>
  <files>src/main.tsx, src/App.tsx, src/router.tsx, src/layouts/AppLayout.tsx, src/pages/DashboardPage.tsx, src/pages/TasksPage.tsx, src/pages/ClientsPage.tsx, src/pages/FinancesPage.tsx, src/pages/AgentsPage.tsx, src/pages/TimePage.tsx, src/pages/ReportsPage.tsx, src/pages/NotFoundPage.tsx, index.html</files>
  <action>
    1. Delete ALL files in src/components/ directory (old components being rebuilt from scratch)

    2. Create src/router.tsx:
       - Import { createBrowserRouter } from 'react-router-dom'
       - Define routes:
         / → DashboardPage (within AppLayout)
         /tasks → TasksPage
         /clients → ClientsPage
         /finances → FinancesPage
         /agents → AgentsPage
         /time → TimePage
         /reports → ReportsPage
         * → NotFoundPage
       - Use AppLayout as the parent layout route for all authenticated pages
       - Export router

    3. Create src/layouts/AppLayout.tsx:
       - Minimal shell with Outlet for now (full sidebar/header built in Phase 3)
       - Import { Outlet } from 'react-router-dom'
       - Simple flex container with a sidebar placeholder (just the nav links) and main content area
       - Sidebar links to: Dashboard (/), Tasks (/tasks), Clients (/clients), Finances (/finances), AI Agents (/agents), Time (/time), Reports (/reports)
       - Use NavLink from react-router-dom for active state styling
       - Keep it functional but minimal — Phase 3 will build the real design system
       - FableDash branding in sidebar header

    4. Create page stubs (each as a simple component with page title and "Coming soon" placeholder):
       - src/pages/DashboardPage.tsx — "CEO Dashboard"
       - src/pages/TasksPage.tsx — "Task Management"
       - src/pages/ClientsPage.tsx — "Client Management"
       - src/pages/FinancesPage.tsx — "Financial Performance"
       - src/pages/AgentsPage.tsx — "AI Agents"
       - src/pages/TimePage.tsx — "Time Tracking"
       - src/pages/ReportsPage.tsx — "Health & Vitality Reports"
       - src/pages/NotFoundPage.tsx — 404 page with link back to dashboard
       Each page: export default function PageName() with a simple h1 and description paragraph.

    5. Update src/App.tsx:
       - Import { RouterProvider } from 'react-router-dom'
       - Import router from './router'
       - Render <RouterProvider router={router} />
       - Remove ALL old page imports and sidebar/layout code

    6. Update src/main.tsx:
       - Keep StrictMode wrapper
       - Import App and render it
       - Keep index.css import

    7. Update index.html:
       - Change title to "FableDash — Operations Intelligence"
       - Keep the root div and script tag
       - Remove env-config.js script tag if present
  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <done>React app with react-router-dom routing. AppLayout with sidebar navigation. All 7 page stubs + 404 page. Old components deleted. App builds successfully.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `npx tsc --noEmit` passes (or only has non-critical warnings)
- [ ] All page routes defined in router.tsx
- [ ] No old component files remain in src/components/
- [ ] No Supabase or old API references remain
</verification>

<success_criteria>
- Firebase client SDK initialized and exportable
- API client with automatic auth token injection
- React Router with all page routes
- AppLayout with working navigation
- All 7 page stubs + 404 page
- Old components and utils completely removed
- Frontend builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-04-SUMMARY.md`
</output>
