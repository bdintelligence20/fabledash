---
phase: 02-core-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [python-backend/app/models/client.py, python-backend/app/models/task.py, python-backend/app/models/time_log.py, python-backend/app/models/__init__.py]
autonomous: true
---

<objective>
Define Firestore collection schemas as Pydantic models for all core entities: clients, tasks (with statuses, comments, attachments), and time logs. Establish shared enums, constants, and model patterns that all CRUD endpoint plans will import.

Purpose: All three CRUD endpoint plans (02-02, 02-03, 02-04) depend on these data models. Defining them first in a single plan ensures consistent field naming, shared enums (e.g., partner groups, task statuses, priorities), and a single source of truth for Firestore document shapes.
Output: Pydantic models for Client, Task, TaskComment, TaskAttachment, TimeLog, plus supporting enums and a models __init__ exporting everything.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md
@.planning/phases/01-foundation-infrastructure/01-03-SUMMARY.md

@python-backend/app/models/base.py
@python-backend/app/models/user.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Client and shared enum models</name>
  <files>python-backend/app/models/client.py</files>
  <action>
    1. Create python-backend/app/models/client.py:
       - Define PartnerGroup(str, Enum) with values: COLLAB = "collab", EDCP = "edcp", DIRECT_CLIENTS = "direct_clients", SEPARATE_BUSINESSES = "separate_businesses"
       - Define ClientBase(BaseModel) — shared fields for create/update:
         - name: str (required)
         - partner_group: PartnerGroup (required)
         - contact_email: str | None = None
         - contact_phone: str | None = None
         - description: str | None = None
         - is_active: bool = True
       - Define ClientCreate(ClientBase) — used for POST body (inherits all fields from ClientBase)
       - Define ClientUpdate(BaseModel) — all fields Optional for partial updates:
         - name: str | None = None
         - partner_group: PartnerGroup | None = None
         - contact_email: str | None = None
         - contact_phone: str | None = None
         - description: str | None = None
         - is_active: bool | None = None
       - Define ClientResponse(ClientBase) — full document representation:
         - id: str
         - created_at: datetime
         - updated_at: datetime
         - created_by: str (uid of creator)
       - Firestore collection: "clients" (top-level)
       - Add COLLECTION_NAME = "clients" constant at module level

    Follow the same pattern as user.py — str Enums, pydantic BaseModel, no Firestore-specific code in models (that goes in the API layer).
  </action>
  <verify>cd python-backend && python -c "from app.models.client import ClientCreate, ClientUpdate, ClientResponse, PartnerGroup; print('Client models OK')" && cd ..</verify>
  <done>Client models importable. PartnerGroup enum has all 4 partner groups. ClientCreate, ClientUpdate (partial), and ClientResponse models defined with correct fields.</done>
</task>

<task type="auto">
  <name>Task 2: Create Task, TimeLog models and update models __init__</name>
  <files>python-backend/app/models/task.py, python-backend/app/models/time_log.py, python-backend/app/models/__init__.py</files>
  <action>
    1. Create python-backend/app/models/task.py:
       - Define TaskStatus(str, Enum): TODO = "todo", IN_PROGRESS = "in_progress", IN_REVIEW = "in_review", DONE = "done", BLOCKED = "blocked"
       - Define TaskPriority(str, Enum): LOW = "low", MEDIUM = "medium", HIGH = "high", URGENT = "urgent"
       - Define TaskComment(BaseModel): id: str, content: str, author_uid: str, author_name: str | None = None, created_at: datetime
       - Define TaskAttachment(BaseModel): id: str, filename: str, url: str, content_type: str | None = None, uploaded_by: str, uploaded_at: datetime
       - Define TaskBase(BaseModel) — shared fields:
         - title: str (required)
         - description: str | None = None
         - client_id: str (required — every task belongs to a client)
         - status: TaskStatus = TaskStatus.TODO
         - priority: TaskPriority = TaskPriority.MEDIUM
         - due_date: datetime | None = None
         - assigned_to: str | None = None (uid)
       - Define TaskCreate(TaskBase) — used for POST body
       - Define TaskUpdate(BaseModel) — all fields Optional:
         - title, description, client_id, status, priority, due_date, assigned_to — all str | None or enum | None
       - Define TaskResponse(TaskBase) — full document:
         - id: str
         - comments: list[TaskComment] = []
         - attachments: list[TaskAttachment] = []
         - created_at: datetime
         - updated_at: datetime
         - created_by: str
       - Add COLLECTION_NAME = "tasks" constant

    2. Create python-backend/app/models/time_log.py:
       - Define TimeLogBase(BaseModel) — shared fields:
         - date: date (required — the calendar date of the time entry)
         - client_id: str (required)
         - task_id: str | None = None (optional link to a task)
         - description: str (required)
         - start_time: time (required — HH:MM start)
         - end_time: time (required — HH:MM end)
       - Define TimeLogCreate(TimeLogBase) — used for POST body
       - Define TimeLogUpdate(BaseModel) — all fields Optional
       - Define TimeLogResponse(TimeLogBase) — full document:
         - id: str
         - duration_minutes: int (auto-calculated from start_time and end_time — computed at API layer, stored in Firestore for query efficiency)
         - created_at: datetime
         - updated_at: datetime
         - created_by: str
       - Add COLLECTION_NAME = "time_logs" constant
       - Note: duration_minutes is NOT in Base/Create — it is calculated server-side in the API endpoint and stored alongside the document. Include a `calculate_duration_minutes(start: time, end: time) -> int` helper function in this module that calculates minutes between two times (handles simple same-day case; raise ValueError if end <= start).

    3. Create or update python-backend/app/models/__init__.py:
       - Export all public models and enums from client, task, time_log, user, and base modules
       - This gives consumers a single import point: `from app.models import ClientCreate, TaskResponse, ...`
  </action>
  <verify>cd python-backend && python -c "
from app.models.task import TaskCreate, TaskUpdate, TaskResponse, TaskStatus, TaskPriority, TaskComment, TaskAttachment
from app.models.time_log import TimeLogCreate, TimeLogUpdate, TimeLogResponse, calculate_duration_minutes
from datetime import time
assert calculate_duration_minutes(time(9, 0), time(10, 30)) == 90
print('Task + TimeLog models OK, duration calc OK')
" && cd ..</verify>
  <done>Task models with status/priority enums, comments, attachments. TimeLog models with date/start/end/description. calculate_duration_minutes helper works. models __init__ exports all models. All importable without errors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd python-backend && python -c "from app.models.client import ClientCreate, ClientResponse, PartnerGroup; print('OK')"` succeeds
- [ ] `cd python-backend && python -c "from app.models.task import TaskCreate, TaskResponse, TaskStatus, TaskPriority; print('OK')"` succeeds
- [ ] `cd python-backend && python -c "from app.models.time_log import TimeLogCreate, TimeLogResponse, calculate_duration_minutes; print('OK')"` succeeds
- [ ] `cd python-backend && python -c "from app.models import ClientCreate, TaskCreate, TimeLogCreate; print('OK')"` succeeds
- [ ] Duration calculation: calculate_duration_minutes(time(9,0), time(10,30)) == 90
</verification>

<success_criteria>
- All Pydantic models defined for clients, tasks, and time logs
- PartnerGroup enum covers all 4 groups (Collab, EDCP, Direct Clients, Separate Businesses)
- TaskStatus enum covers full workflow (todo, in_progress, in_review, done, blocked)
- TaskPriority enum covers 4 levels (low, medium, high, urgent)
- TimeLog has date, client, description, start/end times
- calculate_duration_minutes helper works correctly
- Create/Update/Response pattern consistent across all entities
- models __init__ provides single import point
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-data-layer/02-01-SUMMARY.md`
</output>
